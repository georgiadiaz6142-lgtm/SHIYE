# 拾页 SHIYE 技术适配声明

> 版本：V1.0  
> 日期：2026-09-08  
> 状态：第一阶段开发中；依赖安装、类型检查、13 项后端测试及 3 组本机 HTTP 模拟流程通过；真实模型验收待完成。  
> 当前授权：第一阶段开发、列明的 6 项项目依赖及必要传递依赖安装、本机联调；付费、照片上传、迁移和部署仍需分别确认。  
> 配套文档：[第一阶段技术开发文档](./拾页-第一阶段技术开发文档.md)、[PROJECT.md](./PROJECT.md)、[PRD](./拾页-PRD.md)、[产品设计文档](./拾页-产品设计文档.md)、[AI 与后端技术方案](./拾页-AI与后端技术方案.md)、[通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)。

本文区分【已确认/现场事实】、【合理推断】、【建议设计】和【未知/尚未确认】。方案写入不等于实现、模型能力、隐私条件或上线验收通过。

## 1. 产品形态判断

### 1.1 产品与核心任务

【已确认/现场事实】拾页是面向手机和电脑浏览器的照片贴纸与手帐创作产品。本阶段主链路为：

**上传照片 → 自动发现多个主要物体 → 分别显示候选 → 修正漏提或错提并自动贴边 → 确认透明贴纸 → 收藏 → 放入手帐。**

核心交互属于分阶段人工确认和空间编辑。采用通用手册的“纵向切片”路径：复用现有工坊、收藏和创作界面，同时建立最小真实后端及分割能力；不另建平行原型或临时验收网站。

### 1.2 当前基线

【已确认/现场事实】2026-09-08 接手检查确认：

| 对象 | 当前事实 |
| --- | --- |
| 代码位置 | `/Users/song/Documents/拾页/shiye-editorial-prototype` |
| 预览地址 | `http://127.0.0.1:4176/`；接手检查时三个入口源文件的 HTTP 内容与磁盘逐字节一致 |
| 认可视觉 | 米白首页、绿色书本；保留 12 种纸样、小剪刀和侧边软纸翻页 |
| 撤回历史 | 木桌版、画笔试验均不得恢复；相关历史截图与测试不代表当前认可版本 |
| 代码形态 | 原生 HTML/CSS/JavaScript；没有正式业务后端，不能称为已完成 TypeScript 全栈项目 |
| 数据 | IndexedDB 本机工作区；包含手帐、贴纸和图片 data URL，已有保存与冲突处理 |
| 分割 | 只有预制素材演示和 Canvas 本地轮廓/矩形裁切，没有真实模型自动发现主体或自动贴边 |
| 未接入 | 账号、正式后端、云同步、真实 AI 等正式能力 |
| 恢复 | 存在代码归档；目录尚未初始化 Git；代码归档不包含浏览器作品 |

最终纸样版本的三个源文件 SHA-256 与 [PAPER-LIBRARY.md](./verification/cream-20260908/PAPER-LIBRARY.md) 一致。接手时语法检查与 10 项数据合并单测通过。纸样专项 4 组、相关回归 8 组是已有记录，其中回归后的纸样小样调整由最终专项覆盖；不表述为本次重新执行全部浏览器验收。真实手机、Safari 和真实分割仍待验证。

### 1.3 阶段范围和历史冲突

本阶段交付为“在现有原型内运行、具备真实分割能力的本地完整流程”，不等于全栈封闭测试版或正式上线。

本轮任务的最新明确要求优先。以下冲突仅记录于新文档，本次未修改原文件：

| 历史内容 | 本阶段采用的解释 |
| --- | --- |
| PROJECT 前部的七天全栈、20～50 人封闭测试和公网部署目标 | 保留为更大范围的历史实施计划；本阶段先验证分割闭环，不承诺全部交付 |
| 产品设计文档部分章节要求重新建立 UI | 保留当前认可视觉，仅为本阶段流程增加必要状态与交互 |
| 部分文档写四种纸样、页角翻页或撤销未确认 | 以现场 12 种纸样、侧边软纸翻页和现有撤销实现为准 |
| 旧验证摘要仍保留修复前失败 | 按后续修复记录、源文件版本和验证范围判断，不能直接当作当前缺陷 |
| 至少一个候选或按照片统计成功 | 增加逐主体召回、候选独立性、错误/重复及纠错质量验收 |
| 通用手册默认 Python/FastAPI | 按本文采用 Node.js/TypeScript，不建设两套业务后端 |
| 早期方案将 veFaaS `/tmp` SQLite 作为备选 | 不将临时目录作为可靠业务数据库；本阶段本地任务存储也不构成生产替代方案 |

## 2. 采用的默认方案

【建议设计】采用模块化单体、一套业务后端、稳定接口和可替换供应商适配器。具体实现统一为：

| 层面 | 方案与原因 |
| --- | --- |
| 现有前端 | 保留 HTML/CSS/JavaScript，保留现有页面和交互；仅逐步整理实际改动边界 |
| 新增业务后端 | Node.js + TypeScript + Express；与既有 JavaScript 代码衔接，集中管理上传、任务与模型代理 |
| 契约校验 | TypeScript 类型 + Zod 运行时校验；类型不能代替对上传和供应商响应的实际检查 |
| 接口边界 | 保留现有技术方案的 `/api` 业务命名；供应商字段不直接侵入编辑器 |
| 长任务 | 提交任务并轮询状态，持久化操作标识、版本和供应商请求编号，显式处理取消及恢复 |
| 图片与作品 | 保留 IndexedDB 兼容性；新增贴纸采用独立 Blob 资产存储，旧 data URL 数据继续可读，不批量覆盖 |
| 本地任务持久化 | 带 schema 版本的元数据文件，单进程串行与原子替换；图片位于私有运行目录，任务不携带图片正文 |
| 测试 | 复用现有合并单测和交互回归，补充接口/任务测试；模拟验证与真实模型验证分别报告 |
| 密钥 | 服务端环境变量或受控 Secret；由 Song 在本机/平台配置，禁止在聊天中发送或回显 |
| 依赖 | 仅引入实际需要的库；安装前提交用途、版本和位置，获准后锁定版本 |

不把新方案表述为已有工程事实。接手时 Node.js 为 v22.22.3，npm 为 10.9.8；现有 Python 3.9.6 用于静态预览，不构成 Python 业务后端。后续安装前重新核验兼容版本。

## 3. 触发的按需模块

### 3.1 本阶段引入的能力

| 模块 | 触发需求 | 实施边界 |
| --- | --- | --- |
| 自动/交互分割适配器 | 多主体发现和漏提/错提修正 | 首轮只接入一个托管供应商；模拟模式明确标识 |
| 图片规范化与蒙版合成 | 方向、坐标、透明边缘与白边一致性 | 使用原图规范化像素和真实蒙版，不生成式重绘 |
| 可恢复任务管理 | 超时、取消、刷新和迟到响应 | 本地单进程，不引入 Redis 或独立队列集群 |
| 独立图片资产存储 | 透明贴纸收藏、引用和重开 | 兼容旧作品，迁移/覆盖前单独确认 |
| 质量与成本记录 | 判断分割是否可用、费用是否可控 | 记录最小必要元数据，不记录照片正文、密钥或完整签名 URL |

### 3.2 当前接入选择：百度智能抠图

【已确认/最新决策，2026-09-08】Song 无法使用原建议的 fal，选择先验证百度智能抠图，已在本机配置密钥并授权继续接口开发。因此本轮只建设百度适配器，仍为一套 Node.js/TypeScript 业务后端。原 fal 建议属于历史调研，不再作为当前开通路径。

【已确认/官方页面事实】百度支持自动抠图和框提示，返回一张前景或蒙版，没有公开的正负点或圈线字段。本次请求灰度蒙版后用原图像素合成透明贴纸；框选仅作模型提示。[百度 API 文档](https://ai.baidu.com/ai-doc/IMAGEPROCESS/rm8zl3koj)

【已确认/工程事实】已实现自动前景、框选补提/重提、透明预览和收藏路径，并完成离线接口及本机浏览器验证。默认保持模拟模式，没有向百度鉴权、上传图片或发起真实推理。两项密钥仅确认已填写，尚未验证有效性。

【合理推断/能力缺口】一张前景可能包含多个物体，不能当作多个独立候选；框提示也不能代替正负点纠错能力。当前展示一个自动前景候选，允许框选后新增或替换单个候选；不自动拆分蒙版来假称发现多个主要物体。

【尚未确认】真实边缘质量、独立主体提取与纠错效果。该接入是能力小样准备，不改变第一阶段多主体和交互验收要求，不以手工裁切或生成式重绘补位。

### 3.3 费用和图片政策门槛

- 控制台 API 免费次数、有效期及后付费状态待确认；网页体验额度不代表 API 额度。
- 真实模式需显式批准、调用尝试上限、授权截止时间和本地临时保留期限，且每张照片需主动确认发送给百度。
- 不自动重试百度请求；取消只表示不再采纳结果，超时或重启不能推断未调用或未计费。
- 百度端处理地区、完整保留/删除和训练使用政策仍待核准；不能承诺无留存、无训练或立即删除。本地 TTL 不等于供应商删除策略。
- 首轮仅使用 Song 明确授权发送给百度且有使用权的非敏感测试图片；密钥配置不等于照片传输、付费或部署授权。

具体代码、配置门槛和证据见[百度接入记录](./verification/baidu-20260908/BAIDU-INTEGRATION.md)。

## 4. 偏离或暂缓的默认方案

【建议设计】

| 默认或更大范围方案 | 替代/暂缓、原因与影响 | 重新评估条件 |
| --- | --- | --- |
| Python/FastAPI/Pydantic/pytest | 使用 Node.js/TypeScript/Express/Zod 和现有测试体系。托管分割不要求自建 Python 推理；减少两套业务栈维护成本 | 确实需要自托管模型或只能由 Python 实现的能力时，另行评估推理服务，不自动复制业务后端 |
| 新建 Next.js/React/Tailwind 前端 | 保留现有前端，避免推翻已认可视觉与交互 | 真实闭环通过后，代码规模确有维护需要且迁移范围获准 |
| 后端先行再做临时验收界面 | 采用现有 UI 的纵向切片 | 当前人工纠错与画布操作本身就是验收对象，无须另建页面 |
| 默认 `/api/v1` | 延续文档中的 `/api`，版本变化另行规划 | 存在真实外部调用方或不兼容演进需求时 |
| 正式云数据库、私有对象存储直传 | 本阶段仅本机任务元数据和私有运行文件；正式云合同保留，模型传输另行核准 | 账号、多人访问、云同步或部署前必须完成正式方案 |
| Redis、独立 Worker、向量库、RAG、OCR、动态 Agent | 未被当前链路触发，不引入 | 有明确业务需求、容量证据和对应授权时 |
| 账号、会员、管理后台、AI 文案/排版、云同步和发布 | 暂缓到后续阶段；不删除已确认产品要求 | 分割闭环真实验收通过，再确认下一阶段范围 |

本地模式只绑定回环地址并限制允许的请求来源；它不具备正式账号隔离能力，不得直接开放到局域网或公网。

## 5. 强制底线检查

| 底线 | 当前状态 | 阶段要求 |
| --- | --- | --- |
| 密钥与隐私 | 两项百度密钥已本地填写；有效性、额度及政策未验证 | 密钥仅服务端；上传先授权；落实访问、保留、清理和日志最小化 |
| 数据与任务可恢复 | 部分已有、整体待处理 | 保留原型保存保护；新增任务恢复与资产兼容；代码恢复点和作品备份分别验证 |
| 输入输出校验 | 待实现 | 检查图片实际类型、尺寸、方向、坐标与蒙版；禁止仅信任扩展名或类型声明 |
| 错误与日志边界 | 待实现 | 统一错误类型；迟到响应失效；不盲目重提可能已计费的任务；日志不含敏感正文 |
| 测试与真实模型验收 | 计划已记录，尚未完成 | 第一层模拟流程测试，第二层真实样本评测；两者均通过才能判定本阶段完成 |
| 视觉与已有数据 | 已有基线，开发后仍需回归 | 保留米白/绿书、12 纸样、小剪刀、软翻页及旧作品；不恢复撤回方案 |

没有 Key 可以进行经授权的模拟开发，但不能完成真实分割验收。预制贴纸、矩形裁切、手工轮廓裁切和生成式重绘均不得计入真实模型通过结果。

## 6. 需要 Song 决定的问题

Song 已在文档写入后明确授权进入开发，无需再次确认启动或选择框架。本轮已单独批准列明的 6 项项目依赖及必要传递依赖安装、本机联调；不将该授权扩大为上传或付费授权。

以下在相应动作前提交具体内容确认，不将文档授权扩展为执行授权：

- 依赖安装：库名、用途、版本、项目内位置；不全局安装或升级运行时。
- 供应商开通和费用：平台、具体端点、实际价格、预算上限与停止条件。
- 照片传输：明确素材清单、接收方、处理地区、访问方式、保留和删除安排。
- 旧数据迁移、删除、移动或覆盖：精确对象、影响和可恢复方式；默认保留旧作品与代码归档。
- 部署或扩大访问范围：属于后续独立授权，本阶段不执行。

数值验收目标和可操作清单见配套开发文档；目标尚未经过真实模型验证，未达标不得自行降低或标记完成。

## 7. 历史实施进度：本机 HTTP 模拟联调（2026-09-08）

依赖安装与严格 TypeScript 编译已完成，13 项后端测试和 3 组真实本机 HTTP 浏览器流程通过。后端仍只支持合成测试图；真实 fal 适配器未实施，拒绝个人照片和 live 模式。初次实施记录中的“依赖未安装/后端未运行”为历史状态。详见[本机联调记录](./verification/stage1-20260908/HTTP-INTEGRATION.md)。这些结果不代表第一层工程要求全部覆盖或真实模型验收通过。

### 最新补充：百度接入工程准备

百度适配器及现有工坊接入已实施，离线后端和隔离浏览器检查通过；未进行真实模型调用，第一阶段仍未完成。当前以[百度接入记录](./verification/baidu-20260908/BAIDU-INTEGRATION.md)为准，上述仅支持模拟的描述是此前状态。
