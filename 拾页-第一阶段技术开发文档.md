# 拾页 SHIYE 第一阶段技术开发文档：真实分割到手帐闭环

> 版本：V1.0  
> 日期：2026-09-08  
> 状态：第一阶段开发中；依赖安装、类型检查、13 项后端测试及 3 组本机 HTTP 模拟流程通过；真实模型验收待完成。  
> 当前授权：第一阶段开发、列明的 6 项项目依赖及必要传递依赖安装、本机联调；付费、照片上传、迁移和部署仍需分别确认。  
> 配套文档：[技术适配声明](./拾页-技术适配声明.md)、[PRD](./拾页-PRD.md)、[产品设计文档](./拾页-产品设计文档.md)、[AI 与后端技术方案](./拾页-AI与后端技术方案.md)、[PROJECT.md](./PROJECT.md)、[通用技术栈手册](./AI产品Vibe%20Coding通用技术栈手册.md)。

除注明【已确认/现场事实】外，本文接口、结构、阶段产物和验收要求均为【建议设计】，不是已有可调用能力。本文落实已提交的接手方案，不替代正式全栈产品的其余要求。

## 一、阶段目标

在 `shiye-editorial-prototype` 中打通：

**上传照片 → 自动发现多个主要物体 → 独立候选预览 → 对漏提/错提对象交互修正并自动贴边 → 确认透明贴纸 → 收藏 → 放入原有手帐。**

交付物应包含唯一业务后端、真实供应商适配器、现有页面内的完整流程、可恢复任务、兼容旧作品的贴纸存储，以及可复核的模拟/真实测试证据。

保持米白首页、绿色书本、12 种纸样、小剪刀和侧边软纸翻页。木桌版与画笔试验不恢复；不建设另一套原型，不迁移整套前端框架。

本阶段不实施账号、会员权益、云同步、后台、AI 文案/排版或部署；不新增用户作品导出、绘画工具或自然语言选主体能力。正式产品要求继续保留，不能以本阶段通过宣称全栈产品完成。

## 二、技术适配摘要

- 开发路径：现有 UI 的纵向切片，交互纠错和人工确认与后端同时验证。
- 唯一业务栈：Node.js + TypeScript + Express + Zod；保留原生前端。
- 模型路径：按 Song 最新确认先接入百度智能抠图，验证自动前景和框提示；独立多主体与点选纠错仍有能力缺口。不同时建设第二套供应商或 Python 业务后端。
- 按需能力：图片规范化、真实蒙版合成、任务恢复、独立 Blob 资产、最小质量/成本记录。
- 暂缓基础设施：正式云数据库、对象存储直传、独立队列、账户体系及部署。单机文件方案不替代生产存储合同。
- 当前状态和文档冲突以[技术适配声明第 1 节](./拾页-技术适配声明.md)为准；本次不修改旧文档。

## 三、技术栈与模型

| 组件 | 具体职责 | 当前状态 |
| --- | --- | --- |
| 现有 HTML/CSS/JavaScript | 工坊、候选、收藏、创作和阅读 | 已有原型；真实分割流程待接入 |
| Node.js/TypeScript/Express | 本地 API、任务、模型代理、受控文件访问 | 工程与依赖已就绪；本机模拟 HTTP 验证通过 |
| Zod | 请求、持久化元数据及供应商响应校验 | 已安装；真实供应商响应适配待实现 |
| IndexedDB | 本机手帐与贴纸资产、兼容旧数据 | 已有工作区；新增 Blob 结构待设计实施 |
| 百度智能抠图 | 自动前景、框提示、灰度蒙版；独立多主体与点选不保证 | 接入代码与离线验证完成；本地密钥已填写，真实调用未验收 |
| Node 测试工具及现有浏览器验证脚本 | 任务/数据测试与隔离浏览器回归 | 复用可用环境，新增依赖前先确认 |

接口事实及政策依据见技术适配声明第 3 节。SAM 2 的区域输出不保证主要物体语义，候选筛选和轮廓提示转换均需实测。

不为接口齐全而加入视觉大模型或生成式模型。若单一方案不能通过多主体或纠错验收，应提交失败证据和影响，再决定是否调整供应商或增加必要能力。

## 四、环境与配置

### 4.1 当前环境与启动边界

【已确认/现场事实】接手时 Node.js v22.22.3、npm 10.9.8、Python 3.9.6、Git 可用；项目未初始化 Git。现有预览地址为 `http://127.0.0.1:4176/`。

当前已新增 package.json、服务入口和脚本；安装获准后已运行 `npm run build`、`npm test` 和 `npm start`。这些命令已在本轮本机联调执行成功；npm start 通过 SHIYE_PORT=4177 与独立运行目录启动，4176 原预览未替换。最终开发预览优先保持该 origin，避免作品因换端口表现为消失；替换当前监听进程前检查归属和使用状态，不擅自终止现有服务。

后端仅绑定 `127.0.0.1`，同源提供明确允许的静态资源与 `/api`。检查 Host/Origin 和变更请求来源，禁止宽泛 CORS；无账号模式不用于多用户或公网访问。

### 4.2 配置约定

| 配置 | 用途 | 约束 |
| --- | --- | --- |
| `SHIYE_SEGMENTATION_MODE` | `mock` 或 `live` | 默认模拟；无 Key 时真实模式明确不可用，不静默生成假结果 |
| `SHIYE_HOST` / `SHIYE_PORT` | 回环监听与预览端口 | 计划为 `127.0.0.1:4176`，切换前检查冲突 |
| `SHIYE_RUNTIME_DIR` | 私有临时图片与任务元数据 | 位于静态根目录之外；不得将项目根目录整体暴露 |
| `SHIYE_TEMP_TTL_SECONDS` | 临时数据有效期 | 数值待照片政策确认，真实模式不得默认为无限保留 |
| `SHIYE_MODEL_TIMEOUT_MS` | 请求等待/查询边界 | 超时不等于供应商已停止或未计费 |
| `SHIYE_MAX_INFLIGHT` | 本机并发控制 | 初始串行，控制请求数量与成本 |
| `SHIYE_TEST_BUDGET` | 经确认的测试预算 | 实际计价与金额待确认；成本不明时不继续发起可能超预算的新请求 |
| `BAIDU_API_KEY` / `BAIDU_SECRET_KEY` | 百度服务端鉴权 | 仅由 Song 本机配置；不在前端、聊天、报告和日志中出现 |
| `SHIYE_BAIDU_APPROVED` / `SHIYE_BAIDU_MAX_CALLS` / `SHIYE_BAIDU_APPROVED_UNTIL` | 真实小样授权、尝试上限、截止时间 | 默认不启用；配合显式本地 TTL 及逐张照片确认 |

未来 `.env.example` 只包含非秘密配置和空值；真实配置位于静态根目录外并排除出版本控制与归档。禁止读取并打印已有秘密以证明配置成功。

## 五、项目结构

以下为本阶段职责规划；server、shared、模拟工坊模块和验证目录已建立，依赖及运行目录尚未启用：

| 位置（相对项目根目录） | 职责 |
| --- | --- |
| `shiye-editorial-prototype/` | 保留唯一产品前端、现有素材和视觉 |
| `server/` | 同一项目中的唯一业务后端，划分上传、任务、分割适配器和受控媒体访问 |
| `shared/` | 图片会话、任务、候选和错误契约；避免客户端依赖服务器秘密配置 |
| `.local/shiye/` | 私有运行目录建议位置；任务元数据与临时媒体分开，不作为静态资源暴露 |
| `verification/` 下的新阶段目录 | 隔离测试、非敏感结果摘要、版本指纹和验收报告；不覆盖旧证据 |

新增文件前检查同名对象与并发改动。包管理清单只建立一套；不全局安装。代码恢复节点沿用既有保护方式，是否初始化 Git 在开发准备时明确处理。

## 六、数据、资产与状态

### 6.1 最小逻辑数据契约

下表字段是拾页内部设计，不是供应商已提供的字段。

| 对象 | 关键字段 | 约束 |
| --- | --- | --- |
| 图片会话 | `schemaVersion`, `imageSessionId`, `sourceRevision`, `objectKey`, `width`, `height`, `expiresAt` | 方向和尺寸规范化后建立唯一坐标空间；换图递增版本或新建会话 |
| 任务 | `jobId`, `operationId`, `kind`, `status`, `imageSessionId`, `sourceRevision`, `promptRevision`, `providerRequestId`, `createdAt`, `updatedAt`, `expiresAt`, `error` | 元数据原子写入；不存图片正文或带秘密的 URL；单次业务操作幂等 |
| 候选 | `candidateId`, `candidateRevision`, `imageSessionId`, `sourceRevision`, `maskRef`, `boundingBox`, `qualityScore?`, `expiresAt` | 每个候选对应独立真实蒙版；缺少质量分数时不伪造 |
| 纠错提示 | `targetCandidateId?`, `outlinePoints?`, `positivePoints?`, `negativePoints?`, `box?`, `promptRevision` | 坐标均在统一的归一化原图空间；服务端转换为供应商像素坐标 |
| 本地贴纸 | `assetId`, `sourceCandidateId`, `sourceRevision`, `maskRevision`, `blobKey`, `width`, `height`, `border`, `createdAt` | 确认后生成稳定资产；独立 Blob；仅保存必要来源关系，不长期保存完整原图 |
| 使用目的地 | `bookId`, `pageId` | 使用稳定 ID，不依赖可能变化的页序号 |

### 6.2 图片与蒙版

1. 校验 JPEG/PNG/WebP 的实际文件内容、解码尺寸和资源消耗；沿用原型 10 MiB 输入上限作为起点，按供应商限制进一步核对，不静默更改产品承诺。
2. 明确处理方向信息、显示留白、缩放及设备像素比。当前原型最长边 1400 的预处理不是模型质量已通过的分辨率标准；模型输入尺寸通过样本评测决定。
3. 一切提示映射回同一规范化图像空间；换图和窗口变化不应产生坐标漂移。
4. 校验蒙版格式、尺寸、数值范围及非空有效区域。蒙版分辨率不同必须按明确变换对齐，不直接拉伸错位使用。
5. 自动候选进行重复和碎片处理，记录筛选规则与供应商限制；不沿用本地裁切数量上限静默截断主要物体。
6. 透明贴纸从规范化原图像素与真实蒙版合成；允许必要的方向处理与缩放，不重绘主体，不补造遮挡部分。棋盘格仅作预览，不写进图片。
7. 白边在本地确定性处理，保留每枚贴纸设置，并保持预览与实际 PNG 一致；白边调整不调用模型。此处不新增用户作品导出功能。

### 6.3 任务状态与恢复

业务状态仅为 `queued`、`running`、`succeeded`、`failed`、`cancelled`、`expired`。进度阶段和取消请求标志是附加字段，不另造含糊的成功状态。

- 创建任务先持久化元数据；获取供应商请求编号后立即持久化，再继续查询。
- 相同 `operationId` 和相同请求内容返回原任务；相同 ID 携带不同内容返回冲突。
- `queued → running → succeeded/failed`；取消后的迟到结果不生效。临时结果过期后进入 `expired` 或明确不可再读取。
- 前端收到结果需匹配图片会话、源版本、提示版本和目标候选版本；换图、重画或取消后旧结果不改写候选。
- 进程重启后，有供应商请求编号的任务查询原请求；没有编号且是否已提交不明的任务明确报告状态不确定，禁止盲目重新提交可能重复计费的请求。
- `cancelled` 表示本地不再采纳结果，不承诺供应商已经停止计算、费用为零或自动退款。
- 刷新后恢复有效任务与候选；确认过的贴纸与作品依靠本地资产恢复，不依赖临时供应商 URL。
- 原子文件方案要求单进程、串行写入、损坏检测与明确失败；多人并发或云部署前必须重新落实持久数据库。

### 6.4 收藏、兼容与清理

新贴纸 Blob 与资产元数据事务保存成功后才能进入手帐；加入原页面沿用既有保存和多标签页冲突保护。入库或页面保存失败留在可重试状态，不假报成功。

旧 IndexedDB 的 data URL 资产与作品继续可读。新增结构必须有 schema 版本和旧字段兼容；不得清空数据库、强制覆盖工作区或为回退代码删除新作品。需要迁移已有内容时，先提供备份、影响与恢复方案并取得确认。

浏览器 origin 保持不变；使用隔离浏览器与独立数据验证，禁止用 Song 的作品做破坏性测试。现有代码归档不包含浏览器作品，应分别验证恢复方式。

临时原图、蒙版、预览与供应商媒体按批准策略清理；收藏成功后保留贴纸资产，不将完整原图转为长期云端作品。自动清理只作用于新增受管临时对象，不触碰旧作品或归档。

## 七、API 设计

本节是待实现的最小本地接口契约。正式云上传的私有对象存储直传和账号归属合同继续保留，不能把本机临时上传端点当作生产替代品。

| 方法与路径 | 输入 | 输出与约束 |
| --- | --- | --- |
| `GET /api/health` | 无 | 服务/配置可用性；不返回密钥或敏感路径 |
| `POST /api/uploads/photo/init` | 文件元数据、会话标识、`operationId` | 临时 `objectKey`、短期本地上传许可、有效期；不消耗 AI 额度 |
| `PUT /api/uploads/photo/{objectKey}` | 获准文件字节及上传许可 | 校验后的图片会话与规范化尺寸；只能写入本会话临时对象 |
| `POST /api/segmentation/jobs` | `imageSessionId`, `objectKey`, `sourceRevision`, `operationId` | `202` 和 `jobId/status`；开始自动发现；不接受任意用户 URL |
| `GET /api/jobs/{jobId}` | 任务标识 | 状态、进度阶段、有效候选或标准错误；只读查询不发起新的模型推理 |
| `POST /api/jobs/{jobId}/cancel` | `operationId` | 本地取消状态及供应商取消结果是否已知；不承诺零成本 |
| `POST /api/segmentation/refine` | 会话/版本、可选目标候选、轮廓/点/框、`operationId` | `202` 和新任务；结果只更新对应候选或新增遗漏对象 |
| `GET /api/media/{mediaId}` | 受控媒体标识与有效本机会话 | 流式读取获准临时结果；不接受磁盘路径，不暴露目录列表 |

响应保留 `requestId`、`operationId` 及相关源/提示版本。服务器控制对象与会话归属，即使当前无正式账号也不能通过猜 ID 读取其他本地会话文件。

标准错误至少包含 `errorType`、`retryable`、`retryAfter?` 和不含敏感信息的说明，覆盖：输入无效、图片过大、未配置、能力不支持、来源不匹配、过期、限流、超时、供应商失败、无效蒙版、存储失败及操作冲突。HTTP 状态与业务错误对应，前端不解析供应商原始文案决定行为。

二进制上行/下行设大小和时间限制；供应商返回的媒体 URL 在受控下载策略下处理，禁止任意内网/本机地址访问。真实上传方式须在图片政策批准后落实。

本阶段只记录供应商实际成本，不实施正式用户额度账本，不更改 PRD 已确认的用户权益。不能把技术重试自动解释为新的用户消费。

## 八、提示与模型输出设计（原阶段目标，百度限制见最新补充）

本阶段无自由文本 Prompt、对话 Agent 或工具规划器。使用结构化图像提示：

- 自动发现：规范化图像及经评测固定的分割参数。
- 修正已有对象：目标候选标识、正/负点、可选粗圈选与范围提示。
- 新增遗漏对象：不带目标候选，提供该对象提示；不替换其他已成功候选。

小剪刀仍表达“这里是我想要的对象”。圈线是模型提示，不是最终剪切边界。供应商适配器将归一化轮廓转换为其支持的提示，并通过粗圈选、相邻物体、背景误选和细边缘样本验证。

fal 的托管接口没有原生轮廓或输入蒙版字段，不能在文档或 UI 宣称原生支持。若等价提示验证失败，明确显示该修正能力不可用并报告阻断，不用直接裁切、预制贴纸或生成式重绘补位。

供应商返回后由后端校验和规范化，再送前端预览。模型结果不自动成为收藏资产，不直接覆盖手帐内容。

## 九、现有界面内的实施顺序

| 步骤 | 开发与产物 | 完成边界 |
| --- | --- | --- |
| 0. 文档落地 | 两份方案文档 | 已完成，随后获准进入开发 |
| 1. 开发准备 | 获准后建立恢复点、接口契约和所需依赖清单 | 安装前确认；保护现有浏览器作品 |
| 2. 无 Key 工程链路 | 模拟任务、候选、纠错、透明预览、收藏和原页面回填；补充异常处理 | 页面明确模拟；只验证流程和数据，不计分割质量 |
| 3. 真实能力小样 | 供应商、价格、政策及素材批准后，先用少量授权图片测试自动与纠错 | 关键能力不成立时先停止扩大开发，给出真实失败证据 |
| 4. 完整真实评测 | 至少 50 张代表性样本、纠错案例、成本和时延统计 | 模拟结果不能混入；按第十节门槛判定 |
| 5. 兼容与回归验收 | 现有视觉/交互、旧作品读写、异常与恢复、目标浏览器 | 列明真机/Safari 等未测项，不自动扩大到部署 |

工坊默认主路径为真实自动发现；没有可用服务时如实说明，不伪装正在识别。原本地裁切若保留，只作为明确标记的本地试用，不作为真实流程成功兜底。

## 十、测试要求与通过标准

### 10.1 第一层：模拟和工程测试

模拟模式只使用明确标记的测试数据。几何蒙版可用于验证 alpha、坐标或状态机，但不证明模型分割质量。

| 场景 | 必须证明 |
| --- | --- |
| 多候选、无候选、部分无效 | 独立选择与提示正确；无效结果不进入资产 |
| 换图、重画、取消、迟到响应 | 旧结果不修改当前会话或新修正 |
| 同一操作重复点击/重试 | 不重复创建任务或入库；无法确认是否计费时不盲目重发 |
| 正负点、粗轮廓、方向与缩放 | 坐标映射正确，目标候选之外的数据保持不变 |
| 无 Key、限流、超时、不支持和无效蒙版 | 明确错误和重试边界，不静默使用预制结果 |
| 刷新和进程重启 | 恢复已知任务；无法恢复时明确失败且不丢已保存作品 |
| 存储配额失败和并发更新 | 阻止假成功，保留可重试内容和原有合并/冲突保护 |
| 输入与文件访问 | 阻止伪装文件、超限解码、越权对象、路径穿越、任意 URL 抓取及静态秘密泄露 |
| 旧作品兼容 | 旧 data URL 资产、书页与 12 纸样继续可读写；模拟异常不触碰用户作品 |

### 10.2 第二层：真实模型验收

先小样确认端点能力，再完成至少 **50 张**具有可验证使用权且获准传输的代表性图片；其中至少 **20 张**含多个主要物体。覆盖人物、动物、建筑、食物、日常物品和混合主体，以及相邻、遮挡、低对比、复杂背景、方向异常和细边缘。

测试前标注“应作为独立候选出现的主要物体”和难点；不能看到输出后再删掉漏提对象。固定端点、模型版本（若可得）、参数、样本编号和代码版本。抽取至少 **20 个**漏提/错提对象验证纠错；不足时另加标注挑战样本，自动发现统计仍使用冻结的原测试集。

| 指标 | 本阶段验收目标与口径 |
| --- | --- |
| 自动发现召回 | 独立候选与预标注物体一对一匹配，成功发现物体数/应发现物体数 **≥90%**；仅返回一个前景合并图不算分别发现 |
| 错误/重复候选 | 错误或重复候选的去重计数/返回候选总数 **≤10%**；同时报告空结果与漏提，不能靠少返回刷分 |
| 交互纠错 | 至少 20 个对象中 **≥90% 在两轮内**达到可用；一轮为提交一次用户修正并得到一次模型结果 |
| 可用贴纸 | 对计为成功的对象，关键部位无明显切断、相邻主体可分别选择、无明显背景残留/黑边；细发丝等按样本记录人工判断，不承诺任意照片完美 |
| 真实来源 | 每张成功贴纸可关联真实请求、输入版本与模型蒙版；无手绘边界直接裁切、矩形替代或生成式重绘 |
| 数量与文件 | 选中 N 个对象得到 N 张可分别收藏的透明 PNG；棋盘格不进入文件；白边预览与文件一致 |
| 时延 | 自动发现和单次修正分别统计；从代理接受请求到候选可用的 **P95 ≤15 秒**，包含排队/处理，不含用户思考；超时另报，不从成绩中静默排除 |
| 成本 | 自动、修正、失败或不可用结果分别记录真实成本；实际预算与收费单位在开通前确认 |

90%、10%、两轮等是接手方案提出并在本文记录的阶段目标，尚无真实数据支持已达成。所有类别分别报告；关键流程不支持不能被总平均掩盖。达不到目标时记录失败并重新确认方案，不自行降门槛。

### 10.3 产品与恢复回归

- 收藏后放入指定 `bookId/pageId`，移动、缩放、旋转、保存和重开完整通过；已收藏资产不依赖临时结果链接存活。
- 保存失败不得跳转并提示成功；成功候选、其他页与其他手帐不因修正或换纸被覆盖。
- 米白首页、绿色书本、12 种纸样、小剪刀、侧边软纸翻页、撤销与读写模式均回归。
- 隔离浏览器覆盖桌面和模拟移动；真实移动设备与 Safari 单独记录。未执行的兼容范围保留待验收，不由模拟结果替代。
- 临时文件过期、清理及任务恢复可复核；不能据本地删除宣称供应商全链路删除。

### 10.4 证据与完成判定

工程报告与模型报告分开保存，至少包含日期、运行环境、代码指纹、用例/样本编号、预期与实际、失败明细、供应商请求编号、耗时与费用。敏感照片、密钥、完整签名 URL 不进入公开报告或仓库。

只有工程链路、真实模型质量、数据保护和已约定的兼容验证均通过，才标记第一阶段完成。没有 Key 时只可报告“模拟工程部分完成，真实模型验收待完成”。

## 十一、Song 可操作的验收清单

以下均未执行，不能预先勾选：

- [ ] 打开原 4176 页面，确认仍是米白首页与绿色书本，纸样仍有 12 种，小剪刀与侧边软纸翻页保留。
- [ ] 在批准真实调用后，上传一张有多个明确物体的测试照片，不手动画边，观察各物体分别成为候选。
- [ ] 分别选择和取消两个候选，确认不会把相邻物体当成只能一起选择的一整张前景。
- [ ] 对一个漏提物体用小剪刀粗圈选，再修正提示，确认输出沿物体边缘贴合而不是沿圈线剪切。
- [ ] 对一个错提候选标明要保留或排除的位置，确认其他已正确候选保持不变。
- [ ] 查看透明贴纸，检查细边缘和背景残留；调整白边，确认不重新调用模型。
- [ ] 确认并收藏多张贴纸，放入进入工坊前的手帐页面，移动/缩放/旋转后保存。
- [ ] 刷新并重开手帐，确认贴纸数量、位置和纸样保留，旧作品仍能正常打开。
- [ ] 在任务期间换图或取消，确认旧结果不会突然替换当前内容；刷新后能看到有效任务状态。
- [ ] 在隔离测试中查看模拟断网、超时和保存失败，确认错误明确且没有假成功。
- [ ] 查阅 50 张样本报告、纠错案例、P95 和实际费用，确认结果属于真实调用而非演示素材。
- [ ] 完成已约定的真实手机/Safari 验证，或明确记录尚未通过的范围。

排查问题时只需提供操作步骤、页面提示、发生时间和任务编号；不要发送密钥。涉及原照片或截图时先确认分享范围。

## 十二、风险与待确认事项

| 事项 | 当前结论 | 动作前需要的确认 |
| --- | --- | --- |
| 自动主要物体质量 | 接口具备独立蒙版输出，效果未验证 | 用冻结样本实测，不以官方功能描述认定通过 |
| 粗轮廓自动贴边 | fal 无原生轮廓字段，转换效果未知 | 真实验收失败时决定调整方案，不手工裁切冒充 |
| 服务费用 | 尚无 Key，具体单价/充值门槛待查 | 端点、实际价格、总预算和停止条件 |
| 图片政策 | 地区、完整保留/删除与条款适用仍待核准 | 接收方、获准素材、访问与保留安排 |
| 依赖和运行环境 | 未安装或升级 | 所需依赖用途、版本、位置；不扩大全局范围 |
| 旧作品与恢复 | 代码归档不包含 IndexedDB | 迁移/覆盖前说明精确影响和恢复路径；保留原 origin |
| 开发执行 | 已获准并进入首批代码开发 | 不再确认启动；依赖安装等仍按既定边界执行 |
| 部署与多人使用 | 不属本阶段 | 独立确认正式存储、身份、安全和部署方案 |

## 十三、交接给下一阶段

本阶段完成时应交接可运行代码及确实验证过的启动方式、稳定 API/数据契约、供应商能力与成本报告、真实样本结果、隐私配置、兼容/恢复证据和未解决问题。

下一阶段复用原页面、贴纸资产契约、任务状态和供应商适配器，再落实正式账号、持久数据库、私有对象存储与云同步。用户权益沿用正式 PRD；不得将本阶段的单机状态或供应商成本直接当作正式额度账本。

当前已交付首批源码及前端接口模拟证据，后端与模型仍待验收。后续里程碑完成前不预填“通过”，不以文档存在、模拟成功或代码可启动替代真实验收。

## 十四、当前实现与未完成项（2026-09-08）

- 已写入：单一 TypeScript 后端、Zod 契约、原子任务元数据、模拟候选与透明合成、取消/过期/恢复代码、受控静态文件及会话访问，以及原工坊的模拟交互。
- 已验证：前端接口模拟下的补提/修正、透明预览、独立白边、Blob 收藏重开、保存失败重试及原页回填；模拟移动无横向溢出。该验证不是后端 HTTP 联调或真实分割。
- 新资产在原 `data` object store 中使用 `blob:<assetId>` 键，和工作区元数据同事务写入；旧 data URL 继续可读，不升级 DB 版本或批量迁移，不触碰 Song 的浏览器作品。
- 当前后端仅支持合成测试图，个人照片上传及 live 模式拒绝启用。真实 fal 适配器、费用和图片政策待处理。
- 本轮依赖安装已单独获准并完成；严格 TypeScript 编译、13 项后端测试和 3 组实际 HTTP 浏览器流程通过。4177 运行同一前端的模拟后端，4176 静态预览保留。
- 第一层工程测试尚未全部完成；真实样本、P95、费用、真机及 Safari 仍未验收，不标记第一阶段完成。

具体恢复点、源文件指纹和结果见[第一阶段开发记录](./verification/stage1-20260908/REPORT.md)。

### 交互反馈补充：小剪刀难用（2026-09-08）

Song 同意自动候选/点选修正为主，粗圈选为辅助。已先将现有本地圈选改为画完留在原图、拖点调整、撤销/重做和主动确认预览；尚未完成真实自动贴边，不能据此勾选第十一节的真实模型验收项。完整范围与 4 组专项验证见[交互修正记录](./verification/stage1-20260908/SCISSORS-CONFIRM.md)。

### 本机联调补充（2026-09-08）

已从认可恢复节点继续第一阶段第 2 步，安装精确版本并生成锁文件。修复受控私有媒体读取和烘焙白边重复问题；实际变更、证据和未覆盖项见[本机联调记录](./verification/stage1-20260908/HTTP-INTEGRATION.md)。本轮并未进入第二阶段，也未批准真实照片传输、模型付费或部署。

### 最新补充：百度接入工程准备（2026-09-08）

Song 改选百度并配置本地密钥后，已授权实现接口与工坊接入。历史章节中的 fal 建议和“没有密钥/仅支持合成图”不代表当前代码；真实测试仍未启用。

- 实现 `POST /api/uploads/photo` 二进制本地上传；复用会话、自动任务、框提示修正、透明 PNG 和 Blob 收藏。旧模拟 init 保留，旧 PUT 不开放。
- 百度只返回一张前景，前端明确标注可能含多个物体；框提示不等于矩形裁切，结果必须使用供应商蒙版。百度模式不显示正负点/圈线工具，已认可本地小剪刀不改。
- 密钥有效性、账号免费额度、真实模型质量及照片政策仍待核验；未更改 `.env.local` 的模拟模式，未鉴权或发送照片。
- 本轮只进行离线适配器及真实本机 HTTP 流程检查；独立多主体、两轮纠错、90% 等目标不降低，不将假响应测试记入模型成绩。
- 启用条件、代码恢复点、测试报告和真实验收待办见[百度接入记录](./verification/baidu-20260908/BAIDU-INTEGRATION.md)。
