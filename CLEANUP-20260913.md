# 追加清理结果 · 2026-09-13（当前状态）

Song 在了解三个快照后，明确授权继续清理重复旧备份、抠图/翻页调整前的废弃备份，以及旧测试截图和记录，并要求保留所有当前支撑、重要数据及不能丢失的源码。

## 实际结果

- 项目目录从上一轮清理后的约 **4.2G 降到 340M**；最初约 7.3G。
- `.git/` 从约 **4.0G 降到 126M**；`verification/` 从约 **82M 降到 5.2M**。
- 从本机工作目录删除 **355 个旧测试文件**：216 张图片、139 份原始输出，共 **79,365,171 字节（75.69 MiB）**。
- 从 Git 对象库精确回收 **1,183 个已核验对象**，占用 **4,117,205,453 字节（约 3.83 GiB）**，包括废弃压缩备份、截图、原始输出及对应旧目录对象。未执行全库 prune、reset 或删除整个 `.git`。
- 39 个不同的备份包已流式核对成员清单，没有发现 `.local`、`.git`、`.vefaas`、非示例环境文件、运行目录或上传/作品目录等受保护路径；未解压覆盖任何文件。

## 重要内容如何保留

- 三个 Codex 快照保留，引用指向清理后的目录树；每个快照保留的文件路径、文件模式和 blob 都逐一与原快照一致。只剔除清单中的备份和测试产物。
- 原快照 `acbb0fa6` → `7105fc52`，保留 612 个文件；`ac6afc37` → `de0d6914`，保留 671 个文件；`7f64b8dc` → `cd665e3b`，保留 699 个文件。
- 另外发现旧中间目录对象。为防止丢失其中独有代码，在 `refs/codex/cleanup-preserved/20260913-dc7q63oz` 下保留 99 个旧根目录的清理后内容。原目录到新目录的映射、原始目录结构保存在本机计划清单中；这不是新增一份大压缩备份，底层文件对象仍共享。
- 所有当前源码、素材、依赖、构建、根目录配置以及 36 个本机数据文件均保留；总计 **4,325 个受保护文件**在清理前后 SHA-256 一致。
- 普通分支、标签、HEAD、暂存区、Git 配置及既有 reflog 未变。所有 commit/tag 可达内容（包括未挂接的提交）均受保护。
- 测试脚本、被读取的输入清单和基线、图片样例、近期云端部署记录及配置、重要说明和结论保留。剩余记录并非整目录无差别删除。

## 验证与限制

- Git 连接完整性检查通过；所有公开资源清单中的 140 个路径存在。
- `npm run check:prototype` 通过（16 个脚本检查、10 项合并测试）；`npm run test:frontend` 通过（8 项）；`npm run typecheck` 通过。
- 本次第一次执行在任何 Git 修改或删除之前，因 HTTP 503 停止。随后检查发现本机 4176 没有监听进程；清理前后均如此。没有重启或修改服务，**本轮运行态与云端功能未验证**，上一轮 HTTP 正常结果不能代替本轮验证。
- 本轮没有 Git 提交、GitHub 上传或云端发布。当前隐私文件路径仍未被 Git 索引跟踪；未实施完整历史密钥审计。
- 被回收的废弃包、截图和输出，不再保证可从旧 blob 恢复；部分仍因正常提交/暂存区引用而被保留。快照内保留的源码与素材仍有 Git 恢复来源。
- 清理后的旧报告可能仍记录当时截图、输出和备份路径，这些属于历史证据描述，不表示对应产物仍保留。

本机详细证据：`.local/cleanup-selective-20260913-dc7q63oz/plan.json`、`object-removal-preview.json`、`object-deletions.jsonl`、`working-deletions.jsonl`、`http-before.json`、`http-after.json`。该目录被 Git 忽略。计划清单中的哈希校验是在更新本清理说明之前完成；本说明是验证后有意追加的唯一根目录文件修改。

---

以下为首轮清理的历史记录；其体积、快照状态和恢复说明已由上方追加结果更新。

# GitHub 上传前清理记录 · 2026-09-13

本轮按 Song 的清理请求执行。仅清理已退役备份、未被 Markdown 引用的历史截图和 Finder 元数据，未提交或上传 GitHub。

## 实际删除

| 类别 | 文件数 | 内容体积 |
|---|---:|---:|
| archive 中的旧压缩备份和旧代码副本 | 50 | 2972.26 MiB |
| verification 中的历史截图 | 536 | 148.82 MiB |
| archive / verification 中的 .DS_Store | 3 | 0.04 MiB |

合计 589 个文件、3,272,738,019 字节（约 3.05 GiB）。`du` 实测项目约 7.3G → 4.2G；archive 约 2.9G → 2.5M；verification 约 232M → 82M。

所有备份和截图删除前均以 Git blob 哈希确认存在相同内容的可达 Git 对象。只在文件大小和修改时间仍与清单一致时删除。删除直接作用于原路径，未移入废纸篓。

217 张被文档引用或位于测试输入/回放相关目录的图片保留；测试脚本、结果记录、备份清单与历史说明保留。历史说明中已删除备份或输出图片的路径属于历史记录，不再表示文件仍在工作目录。

## 实际修改与保留

- `.gitignore` 新增 `/archive/`，防止以后将本地旧备份再次加入普通提交。
- 当前 `server/`、`shared/`、`scripts/`、`shiye-editorial-prototype/`、`dist/` 以及包依赖和 TypeScript 配置未改动。
- `.env.local`、`.local/` 原有账号、邀请码和运行数据、`node_modules/`、`.vefaas/` 保留；清理证据新增在已忽略的 `.local/cleanup-20260913-3wf47zfe/`。
- 未改动 `.git/`、分支、标签、提交历史和用户原有未提交代码；未启动、停止或重启现有服务，未读取或清理浏览器作品。

## 实际验证

- 404 个受保护源码、素材、构建和配置文件与清理前 SHA-256 一致。
- 服务端公开资源清单的 140 个路径全部存在。
- `npm run check:prototype` 通过：16 个前端脚本检查、前端类型检查、10 项合并测试。
- `npm run test:frontend` 通过：8 项测试。
- `npm run typecheck` 通过：后端与前端类型检查。
- 当前 `http://127.0.0.1:4176` 的健康接口、首页、app.js、房间首页清理前后均返回 200；健康状态 ok；三个页面/脚本响应的 SHA-256 不变。
- 当前账号、邀请码和环境变量文件已被 Git 忽略，未发现这些路径被当前索引跟踪。这不是对全仓库及历史内容的完整密钥审计。
- 未重新执行浏览器交互、完整后端测试或云端验收；未修改构建产物以避免影响当前运行实例。

## 尚未处理的 Git 工具快照

`.git/` 仍约 4.0G。现场有 3 个 `refs/codex/turn-diffs/checkpoints/` 引用，大备份存在于这些工具快照可达历史中；普通分支的历史 blob 内容合计约 71.3 MiB，没有超过 50 MiB 的 blob。该数字不是压缩后的推送量，也不包含尚未提交的新文件。

这些工具快照是本轮已删除内容的现存恢复来源。进一步删除这 3 个引用并回收其专属对象，会失去对应 Codex 快照及部分旧文件恢复能力，因此本轮保留，待 Song 单独确认。普通分支、标签和提交历史不属于建议删除对象。

逐文件路径、Git blob、删除前大小与时间、受保护文件哈希、工具快照引用、HTTP 前后证据见本机 `.local/cleanup-20260913-3wf47zfe/manifest.json`、`deleted.jsonl`、`http-before.json` 和 `http-after.json`。证据目录被 Git 忽略，不随仓库上传。只要相应 Git 对象仍存在，可按清单中的 blob 提取旧内容到新文件；继续清理 Git 快照后不能保证恢复。
