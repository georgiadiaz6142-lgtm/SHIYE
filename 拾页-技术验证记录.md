# 拾页：第一关技术验证记录

> 更新日期：2026-09-08（Asia/Shanghai）  
> 状态：米白版交互升级完成本地验证；21 项回归、8 项新增交互、7 项专项边界及 10 项合并单测通过；真实 AI／后端待接入，正式技术第一关仍未全部通过。  
> 最新授权范围：Song 已确认原型交互修改与相应文档同步；不部署、不调用付费模型、不上传用户图片，不操作用户现有浏览器作品。下方早期只读限制是历史记录。

## 当前验收对象 · 2026-09-08（以本节为准）

### 最新有效验证结论

2026-09-08 交互升级：以 [INTERACTIONS.md](./verification/cream-20260908/INTERACTIONS.md) 为最新报告。收藏回流、拖入、手柄、横竖文字、双页与正反面、触控模拟均有浏览器证据。最终阅读布局单独补验；没有将本地自由轮廓裁切标记为 AI 自动贴边。以下“四项修复”及更早报告保留为历史。

最新状态（修复后）：Song 已授权修复下述四项问题。产品仅改 `app.js`；最终 [fix-run-02/results.json](./verification/cream-20260908/fix-run-02/results.json) 为 21 通过、0 失败，另 10 项合并逻辑测试通过。变化、截图、使用边界和复跑方式见 [FIXES.md](./verification/cream-20260908/FIXES.md)。已保留修复前节点，米白外观文件、素材、正式产品文档和现有浏览器作品未改。仍运行旧代码的标签页不享有新保护；确认无未保存内容后应加载新版。以下 15 通过／4 失败为修复前记录。

已重新完成米白版的本轮验证，报告见 [verification/cream-20260908/REPORT.md](./verification/cream-20260908/REPORT.md)。照片裁切 → 收藏 → 放入手帐 → 编辑／分页 → 保存 → 刷新重开链路通过，移出已用收藏不破坏手帐。发现 4 个问题：多标签页覆盖已保存作品（数据阻断）、迟到照片响应覆盖当前界面、保存失败仍切翻阅、双击文字无编辑弹窗。真实 AI、正式分区数据层、账号与后端未通过接入验证。未修复产品代码，三份正式产品文档和现有浏览器作品未动。

有效统计仅使用 [run-02/results.json](./verification/cream-20260908/run-02/results.json)：15 项通过、4 项未通过，不是 PRD 总体通过率。身份检查截图已人工确认；run-01 的页面标记／开书等待脚本错误已明确排除，不作为产品失败。以下恢复记录和更早章节保留为历史背景，不替代这份最新米白版报告。

Song 指定的是米白底、左侧品牌／宣传语／“开始创作贴纸”和“观看演示”、右侧绿皮“山野来信”及漂浮照片贴纸的首页。此前仅核对 4176、标题和磁盘响应，漏掉外观核对，实际误测木桌背景版；其通过／失败结果不作为米白版验收依据。

本轮按授权从 `shiye-editorial-prototype/checkpoint-before-desk-20260907.tar.gz` 恢复三份代码，从 `checkpoint-before-home-20260907.tar.gz` 补回缺失咖啡照片。木桌背景图在操作前已不存在；清理剩余来源记录、轻量贴纸副本和两次误测目录。操作前节点为 `archive/2026-09-08-before-cream-restore.tar.gz`，仅供恢复，不作测试入口。浏览器作品和三份正式产品文档未修改。

正式技术第一关仍未通过。恢复与首页检查不等于完整产品验收；下一轮必须先匹配上述外观，再沿该版本测试，真实 AI 能力仍需真实服务证据。

### 本轮恢复验收（不是完整技术验证）

- 隔离 Chrome 152.0.7977.77 直接访问 4176；HTML、JS、CSS 和四张米白版素材共 7 个响应为 200，且与磁盘文件逐字节一致。
- 已人工查看 1440×900 桌面及 390×844 手机尺寸截图：为米白左文右书版，木桌背景不存在；图片无加载失败，390px 无横向溢出。使用减少动态效果设置，不据此评价动效流畅度。
- 实际操作“观看演示 → 下一组 → 合书”及“开始创作贴纸 → 工坊”通过；未观察到未处理 JavaScript 异常。
- 本次只检查恢复目标和首页入口，未进行完整编辑、保存、并发、AI 或真实手机／Safari 验收。隔离浏览器已关闭，没有刷新或清理 Song 现有标签页数据。
- 清理共 33 个文件：木桌来源 JSON 与轻量 WebP 副本 2 个、误测脚本／结果／截图 31 个。两张木桌背景图在本轮开始时已不存在，未恢复。移除前与压缩节点中的对应内容逐字节核验；可从该节点找回。三份正式产品文档哈希保持不变。

## 0. 历史验证对象更正 · 2026-09-07（已被上节补充更正）

【已确认/页面事实】Song 指定的目标是 `http://127.0.0.1:4176/`。直接读取现有标签页后确认：标题为“拾页 SHIYE — 把日子，慢慢收好”，首页有“开始创作贴纸”“观看演示”和合上的“山野来信”。它不是此前测试的根目录旧版页面。**上一轮选择了错误的产品验证对象；7 项通过、3 项未通过以及对应三个问题，均不得作为这个新版的验收结论。**

【已确认/历史现场事实】纠正对象时，监听 4176 的 Python 进程明确使用 `--directory /Users/song/Documents/拾页/shiye-editorial-prototype`，目录当时不存在，`/`、`/app.js`、`/styles.css` 的新请求均为 HTTP 404。原标签仍显示已加载界面，检查时没有刷新、关闭或清理存储。该状态保留为历史，不再代表当前情况。

【已确认/当前现场事实】Song 随后说明曾删除该目录；再次检查时目录已重新出现，首页、JS、CSS 返回 200，响应内容与新版目录文件逐字节一致。因此此前文件缺失/404 阻断已解除。本轮没有执行源文件恢复或覆盖，是在目录已恢复后进行核对和归档。

【已确认/处理结果】经 Song 确认，旧根目录四文件及 `shiye-ui-validation` 已原样移入 `archive/2026-09-07-experiments/`。旧版测试与独立 IndexedDB 试验保留为历史证据，不用于当前新版验收；新版尚无本轮完整功能通过/失败结论。本次归档未修改新版代码、素材、checkpoint、三份正式产品文档或浏览器作品。

历史证据：[当时的 4176 / 404 核对记录](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/target-4176-inspection.json)，原始 JSON 不改写。归档路径及原文件哈希见 [MANIFEST.json](/Users/song/Documents/拾页/archive/2026-09-07-experiments/MANIFEST.json)。下一步是对正确的 4176 新版重测，不再寻找已恢复的目录。

## 1. 旧版与独立试验结论（非 4176 新版结论）

【已确认/历史实测事实】当时根目录的旧原型（现已归档）的基本贴纸操作与文字恢复可运行；独立 IndexedDB 小样本试验证实了结构化页面与透明 PNG Blob 的原子保存、刷新读回、事务回滚和升级失败保留旧数据。**这证明部分前端技术路径可行，不能据此推断新版实现或上线质量。**

【已确认/实测事实】本批 10 项检查为 7 项通过、3 项未通过，复跑结果一致。这不是整个 PRD 的通过率，尤其不包含真实 AI、账号、额度、云同步或生产数据库验收。

【建议设计】保留现有画布的归一化位置、指针拖动、元素变换等思路；不要直接复用其整本书 localStorage 存储和“先显示归架成功、再异步保存”的流程。无需因这批结果推翻产品或重新设计 UI，但必须单独实现正式数据层与视图状态边界。

【已确认/代码事实】这里的“画布”是 DOM 元素定位与 CSS transform，不是已集成的专业 Canvas 编辑引擎。上述通过结果不等于完成编辑器技术选型。尚未验证大图/大量元素性能、多标签页、崩溃/断电、长期容量与淘汰策略、真实手机 Safari、账号加密与解锁、白边生成或跨屏构图一致性。

## 2. 本次到底验证了哪一份代码

- 历史浏览器实测对象：当时项目根目录的 `index.html`、`styles.css`、`app.js`，现位于 `archive/2026-09-07-experiments/`；页面标题为“拾页 SHIYE · 电子手帐原型”。
- 历史只读检查对象：当时的 `shiye-ui-validation/index.html`，现位于上述归档的同名子目录。其中 `runRecognition()`、`markSaving()` 与模型健康检查使用定时器展示演示状态，不能作为真实服务证据。
- 当前新版目录和 HTTP 来源已确认恢复，具体核对见第 0 节；早先询问路径已不再必要。
- 工作区没有 Git 仓库。本轮用源文件 SHA-256 校验修改边界，完整值保存在机器结果中。

## 3. 环境与隔离

- Node.js：22.22.3；环境自带 Playwright：1.62.1；本机 Chrome：152.0.7977.77。
- 桌面视口：1440 × 1000；移动视口：390 × 844，桌面 Chrome 的触控模拟。
- 独立无头浏览器、临时 profile、随机本机端口；没有连接 Song 的现有浏览器 profile，没有读取或清除其中的作品。
- 测试服务只提供三个原型文件与独立存储探针页面，不提供整个目录列表。测试结束关闭测试浏览器及临时服务，没有关闭原有 4176 服务。
- 只使用原型内置 Emoji、测试文字与程序绘制的 32 × 32 PNG，没有上传照片。没有安装依赖、调用模型或创建云资源。
- `agent-browser` CLI 不可用，使用自带 Playwright 替代；普通沙箱不能启动 Chrome，获得执行批准后以隔离浏览器完成实测。

## 4. 实测结果

| 编号 | 验证内容 | 结果 | 可复核证据及边界 |
|---|---|---|---|
| UI-01 | 页面加载与关键控件 | 通过 | 48 个按钮，上传/开书控件存在，初始页未捕获未处理 JavaScript 异常 |
| UI-02 | 添加、拖动、缩放、旋转、删除贴纸 | 通过 | 使用真实鼠标指针及按钮操作；位置、变换和元素数量改变 |
| UI-03 | 添加文字、保存、刷新恢复 | 通过 | 刷新前后页面元素结构一致，文字仍可见；走的是现有 localStorage 路径 |
| UI-04 | 第二页改纸不影响第一页 | 未通过 | 第一页从 `paper-blank` 变为 `paper-grid`；不满足 AC-11 |
| UI-05 | 已有手帐重开默认翻阅 | 未通过 | 重开后新增页面等编辑工具仍可用；不满足 AC-18，是最新规则尚未落地的差距 |
| UI-06 | 保存失败不得成功归架 | 未通过 | 注入 QuotaExceededError 后，本地保存书本数仍为 0，界面书架已有 1 本；不满足 AC-17 |
| UI-07 | 手机尺寸画布与触控选择 | 通过（限定范围） | 页面宽 390、文档宽 390、画布宽 304，触控可选中；不是手机全流程或 Safari 验收 |
| IDB-01 | 页面与 PNG Blob 原子保存、刷新恢复 | 通过（独立试验） | Blob 为 164 字节；读回 SHA-256 一致；PNG 角落 alpha=0、主体 alpha=255；元素引用与 revision 保持 |
| IDB-02 | 跨 store 事务失败回滚 | 通过（独立试验） | 主动 abort 后，无半成品页面、无孤立 Blob，原页面仍在 |
| IDB-03 | schema 升级失败保留旧数据 | 通过（独立试验） | v2 升级主动 abort，数据库仍为 v1，新 store 不存在，旧页面可读 |

【已确认/实测事实】已检查桌面、移动与保存失败截图。它们用于证实页面确实渲染及测试时状态，不代表视觉设计定稿。初始 `pageerror` 为空也不等于所有控制台信息或异常分支均无错误；记录中有测试服务器的 favicon 404，未将它当作业务故障。

## 5. 三个差距的原因与下一步修复方向

### 5.1 纸张放在书级，导致多页联动

【已确认/代码事实】`app.js` 的 `renderPage()` 读取 `state.draft.paper`；纸张按钮也修改该书级字段。页面对象没有独立的 paper 字段。

【建议设计】正式实现使用 `Page.paperId`，只修改当前页。迁移旧数据时将旧书级纸张复制为各页的初始值；迁移验证前保留旧数据。这轮未实施迁移。

### 5.2 重新打开直接暴露编辑能力

【已确认/代码事实】`reopenBook()` 直接展示 editorShell，`attachDrag()` 没有阅读模式保护。界面仅隐藏部分按钮不能替代统一的修改权限检查。

【建议设计】增加明确的 reading/editing 视图状态；已有书加载后先 reading，新建空白书可 editing。元素变换、文字修改、页面操作、AI 应用都检查当前模式。这轮未修改这些函数。

### 5.3 保存失败仍出现已归架内容

【已确认/代码事实】`persist()` 延迟 260ms 写 localStorage、不返回可等待的提交结果；`shelveCurrentBook()` 随即渲染书架并提示成功。异常捕获虽会显示空间不足，但不能撤销先前的成功语义。

【建议设计】存储提交返回 Promise，以事务完成为成功边界；归架先等待提交，失败保留可恢复的编辑内容，不提前显示成功。正式数据层还需将本机保存状态与云同步状态分开。

## 6. 首轮旧版现场检查的 AI 与后端边界（历史范围）

要验证的产品链路为“照片 → 自动多主体候选 → 点/框或一句话补救 → 独立透明贴纸 → 可编辑页面 → 持久化与恢复”。本轮按端到端验证方法检查每个边界；遇到尚不存在的接口后，停止该链路，不把前端演示往下算作成功。

| 能力 | 现场事实 | 本轮状态 / 缺少的证据 |
|---|---|---|
| 自动主体分割 | 根目录 `processPhoto()` 为模拟；`compressImage()` 输出方形 JPEG，没有真实 mask 或模型请求 | 未验证；需要实际分割服务和获准使用的测试照片 |
| 点/框分割、中文指代定位 | 现有文件没有真实后端路由和供应商调用 | 未验证；需主供应商、可用模型、受控密钥配置、调用预算 |
| 文案和排版 JSON | 技术文档有示意结构，验证页有固定文案/布局 | 未验证；需真实模型结果、Schema/语义校验、越界/锁定/旧 revision 等反例 |
| 生产数据库 | 当前项目没有正式后端工程或项目级数据库配置 | 未验证；不能用 IndexedDB 试验代替服务端事务、并发和恢复验证 |
| TOS / 短信 | 没有本项目实际接入和真实请求证据 | 未验证；需要获准使用的环境与实际审核状态，不擅自开通 |
| 账号本地隔离、同图计次、云同步 | 文档规则已确认，原型未实现 | 未验证；需真实鉴权、账本、队列与账号切换测试 |

没有在当前项目发现 `.env*` 或正式 API 工程，**不等于 Song 在其他位置没有云账号或密钥**。本轮没有搜索个人凭据，也没有把公共文档可访问当成模型鉴权通过。

## 7. 公开接口能力核对（与实测分开）

以下是 2026-09-07 查阅官方文档得到的候选接口依据，不是供应商最终选型，也不构成开通、付费或上传授权。

| 需求 | 官方文档中的能力 | 对拾页的判断 |
|---|---|---|
| 自动多主体 mask | fal 的 SAM 2 自动分割接口输出 `individual_masks` 与 `combined_mask`。[接口文档](https://fal.ai/models/fal-ai/sam2/auto-segment/api) | 【合理推断】可作为多候选技术对照；仍需测重复/碎片 mask、漏选、边缘与延迟，不能直接当成合格贴纸 |
| 点/框补救 | fal 的 SAM 2 image 接口接受 `prompts` 与 `box_prompts`，支持应用 mask。[接口文档](https://fal.ai/models/fal-ai/sam2/image/api) | 【合理推断】接口形态匹配交互补救；实际透明通道、坐标映射和精度未验证 |
| 一句话定位、图片文案 | 百炼视觉文档展示物体 bbox/point 定位及图片文案能力。[视觉能力文档](https://help.aliyun.com/zh/model-studio/vision/) | 【合理推断】可作为视觉定位/文案候选；中文指代准确性及排版 JSON 合格率必须另测，不保证模型输出就可直接应用 |

【未知/尚未确认】上述服务的可用账号、实际调用地域、用户照片处理/保留条款、国内网络延迟、最新计费与测试预算。本轮没有调用它们。若后续评测境外服务，先限定为获准的非敏感测试图，不自动发送用户作品；生产采用与否另行确认。

## 8. 历史证据与旧版复跑

- [旧版验证脚本](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-check.cjs)
- [首轮机器结果](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-evidence.json)
- [复核机器结果（含脚本哈希）](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-evidence-repeat.json)
- [桌面画布](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-desktop.png)
- [手机视口](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-mobile.png)
- [保存失败状态](/Users/song/Documents/拾页/archive/2026-09-07-experiments/shiye-ui-validation/technical-save-failure.png)

如确需复跑旧版，在归档目录运行；不要将它当作新版测试：

```bash
cd /Users/song/Documents/拾页/archive/2026-09-07-experiments
node shiye-ui-validation/technical-check.cjs --run-label=your-unique-run
```

每次使用不同 run-label，脚本拒绝覆盖已有证据。测试运行需要允许启动本机 Chrome 和本机临时 HTTP 服务；默认使用当前机器已安装的 Playwright，可通过 `SHIYE_PLAYWRIGHT_PATH` 指向另一已安装版本，不会自动下载依赖。退出码 0 表示本批全部通过，1 表示测试发现差距，2 表示运行环境或脚本中断；以结果 JSON 的明细为准。

## 9. 闸门与下一步（更正后）

【已确认/实测事实】目前只完成 Day 1 的一部分，不能标记“技术第一关通过”，也不能标记正式功能开发完成。代码哈希前后一致，测试数据只存在于隔离环境与证据文件中。

【建议设计】4176 新版文件服务已恢复，下一步在隔离测试环境对这个版本执行完整交互及保存恢复验证，不再使用归档旧版。HTTP 200 和源文件一致性仅是进入该轮验证的前提，不代表功能全部通过。

后续真实 AI 评测仍需确认已有 API 平台、测试图片范围与费用上限；这与本次验证对象错误分开处理，不用缺少 API 环境解释选错页面。不要在对话或本文件记录 API Key。
