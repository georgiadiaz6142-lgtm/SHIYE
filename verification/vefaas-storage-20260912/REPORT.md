# 拾页云端存储验证 · 2026-09-12

## 已确认

- 沿用应用 `shiye-web-test`（862432459a9a）、函数 `ulvzu4v3`、既有独立网关路由；未创建数据库或其他应用。
- 63,391,418 字节完整验证包通过控制台上传，最终在 23.31 / 60.45 MiB 处明确失败。随后精简为 4,619,484 字节，仅独立存储测试所需服务代码与依赖；控制台上传保存成功。
- 测试服务使用平台自带 Native Node 20，健康检查 HTTP 200，实际 Node 20.20.0。正式项目 Node 22 要求未修改，其云端运行仍未验证。
- 发布版本 1 成功，发布记录 `c4vzxldqnls4tlsf`；零预留、实例最小 0／最大 1 设置成功。
- 第一次受保护验证请求返回 HTTP 409，表示该实例验证已经开始，但未获得原任务结果。不能据此确认成功，也不能确定网关是否重复派发；原始响应见 `live-probe.json`。

## 修复与待验证

独立验证入口增加受口令保护的结果查询；同一实例缓存同一个执行 Promise，重复请求不重复执行，失败只返回阶段、合成对象路径及允许的 SDK 错误码。鉴权限制为 ASCII Bearer token，避免多字节输入导致长度异常。5 项针对性 HTTP／凭证测试通过。

本记录不代表完整产品上线。尚未迁移用户作品、账号或 AI 密钥；真实条件写入、清理及跨实例恢复须以后续实际结果为准。验证服务不是对用户开放的产品入口。

## 本轮最终状态

- 结果查询修复包（4,656,327 字节）通过控制台传输至 100% 后迟迟不完成，已取消；未保存到云端。
- 控制台实际代码编辑器为 readonly、保存按钮禁用。尝试输入未改变原代码，核对旧代码后未保存。
- 官方 CLI 对同一精简修复目录再次上传，明确返回 `Error: Upload failed`；随后停止失败进程。最后只读核对函数仍为 `release complete`、代码大小 4,619,484，更新时间 2026-09-11 17:16:32.781 UTC。因此云端仍是最初成功发布的版本 1，结果查询修复仅在本地。
- 一次状态查询的自动审批超时；按提示重试一次后成功。这不是上传失败原因，目前没有等待用户授权的操作。
- 本轮未确认真实 TOS 条件写入、并发保存、新连接恢复或测试对象清理成功；HTTP 409 原因尚未完全定位。不能宣称数据库替代方案验收完成。
- 恢复时先上传本地修复并发布，再使用受保护 GET 查询／POST 新合成验证。旧验证的对象路径没有返回，不得猜测或广泛删除桶内容。临时验证口令按云端设置过期，恢复时应重新生成；不写入报告或源码包。

精简原包 SHA256：`6b81d0fd68ad793061ed190997328f10389ccae26f3df944b9c78bb30b3851b9`。
修复包 `/tmp/shiye-storage-probe-results-20260912.zip`，SHA256：`9822e64327c582017cc3fdd1df66c2af422af7e98b0c83a1862a7bf0dad867f9`。
已解压修复目录 `/tmp/shiye-probe-ready-tsempymr`；本地源文件哈希见 `source-hashes.json`。


## 后续继续：真实存储验证已通过

官方 `UpdateFunction` 的 `SourceType=zip` 直接请求体成功上传修复，版本 2 发布记录 `3rnyzfcun5kzgffa`。先前上传通道失败不代表权限或 TOS 不可用。

版本 2 明确在读取不存在对象时失败（HTTP 404，无 SDK code）。现场 SDK 2.9.1 Node 实现 `getObjectV2` 始终使用 stream 响应，`TosServerError.data` 因而可能是 Readable，直接读 `.code` 得不到错误码。新增有 16 KiB 上限和 2 秒超时的错误流解码，严格只提取错误码；未知、坏数据、NoSuchBucket 仍不能当作对象不存在。

版本 3 前进到重复创建检查，真实返回 HTTP 412 / `ConditionNotMet`；适配器原本只识别 `PreconditionFailed`，现兼容两者，并继续拒绝其他不确定错误。对应运行 `d7ceb64a-8bfe-42de-9f7d-45b3f2fdcbc8` 留下合成 condition.json，尚未清理；不属于用户数据。

版本 4（发布记录 `i18dnr3hgr98iyj0`）真实验证全部通过：create-only、single-writer-cas、new-client-recovery、cleanup，见 `revision-4-probe.json`。这里的恢复是新建 SDK 客户端读取 TOS，不能替代整套应用重启验收。

版本 5（发布记录 `d8ceasehthuag3tj`）向公网入口传入明确无效的合成云凭证头，仍完成同一组真实存储验证，证明此网关入口的平台凭证覆盖生效，见 `revision-5-header-probe.json`。新测试对象已清理。此前版本 1 的原始执行结果未知，仍不据推断标记成功。

存储修复 17 项相关回归通过；其后云端保存状态改为 tos/connected，管理员 API 鉴权测试凭据改为共享存储，跨实例测试／保存及冲突保护共 19 项回归通过。初次云状态 HTTP 测试错误使用 fetch 改写 Host，被正确拒绝；改用原生 HTTP 精确模拟网关后通过，未放宽来源保护。

完整网站包 `/tmp/shiye-full-web-20260912.zip` 已生成，170,153,937 字节，SHA256 `a2d4633af36b714888f3f85c74c2f3ec06b520b97d57d643a9600d1ba6b1714d`；包含 Node 22、Linux 依赖和原始公开前端素材，无环境密钥。直接配置接口上传触发 60 秒超时，随后只读确认云端仍为 2,962,755 字节的验证包。正在使用官方 `GetCodeUploadAddress` 返回的受限地址直传，结果待核对；完整网站尚未发布。

## 2026-09-12 后续：分片部署通道

- 完整原生 TOS 直传 650 秒后超时，仅发送 15,334,882 字节；完整包未落地。记录：`native-full-upload.json`。
- 转为固定哈希清单的分片暂存；临时服务只允许有时限的口令、固定分片编号与哈希，写入 `shiye-verification/deploy-50f26aad4f7628cb9d7743f3973a044bc71868071fac8a296426ef36e916c377/`。
- 临时包裁剪为 1,036,994 字节，本机启动验证通过；发布版本 6，发布记录 `wdcy8ry6aerdas1a`。主入口目前仍为部署暂存服务，不能当作完整网站。
- 163 个 1 MiB 左右的分片支持已确认块续传，当前进度见 `chunk-upload-progress.json`；只对相同固定哈希的缺失确认块重传。
- 创建后台异步函数 `shiye-worker-test` / `9bm54f2f`，0.5 核、1 GiB、按请求计费、重试 0，执行角色 ShiyeTosRuntimeRole；尚未发布。最小／最大实例配置尚未验收。
- 当前后端 108 项测试全部通过，输出 `/tmp/shiye-deployment-current-tests.txt`。
- 准备独立云端加密密钥和初始身份文档；原账号说明文件密码不匹配当前校验值，因此保留现有管理员密码哈希，不更改用户当前密码。账号与配置迁移只在目标状态对象不存在时创建，不覆盖已有云状态。初始文档在本地注入云端启动测试通过，尚未云端写入。
- 云端密钥只保存在权限 0600 的 `.local/cloud-deployment-20260912.env`，不纳入安装包。未上传用户手账图片、贴纸或作品数据。

### 完整包与身份的真实云端结果

- 163 个分片全部确认成功；第一次内存合并后出现 `no_ready_instance`（可能内存不足，未取得平台 OOM 明确记录）。改流式临时文件合并并发布版本 7，发布记录 `b8qoxy7om8j4u2lo`。
- 完整包云端合并成功：170,154,214 字节，SHA256 `50f26aad4f7628cb9d7743f3973a044bc71868071fac8a296426ef36e916c377`；结果见 `assembled-code.json`。
- 账号迁移最初被自动审批阻止；Song 随后明确允许迁移所列账号及配置。POST 初始化返回 201，读回 SHA256 一致，见 `identity-bootstrap.json`。邀请码密文已随新密钥重新加密，并通过本地解密验证。
- 临时源对象为 `vefaas-prod-cn-beijing-3az:code_temp_cache/4d78482e-440e-4ac1-959a-897da5e91a2d.zip`。配置完整待发布代码首次遇 TLS 校验错误，未关闭 TLS 验证，正在重建连接核对。


## 最新：完整网站已公开测试上线

- 网站 https://sm994o19l0g1rajv9qfqf.apigateway-cn-beijing.volceapi.com/ ，网页函数 ulvzu4v3 稳定版本 9、100% 流量。发布记录 9mfsdhftogpnpesf 已返回 done，覆盖此前 CLI TLS 错误造成的未知状态。
- worker 9bm54f2f 稳定版本 2；两函数此前均核对最小 0／最大 1、无预留实例。完整部署源为专用私有桶内 deploy-50f26aad4f7628cb9d7743f3973a044bc71868071fac8a296426ef36e916c377/full.zip，保留供后续发布恢复。
- Song 明确批准的管理员身份及密码哈希、10 个邀请码、1 个用户账号和现有 API 配置已完成仅首次创建的迁移，读回哈希一致。未批量上传本机作品。
- 浏览器实际确认网页 HTTP 200、安静进入可用；现有会话 account 返回 admin，works/status 返回 tos、cloudConnected true。没有自动输入管理员密码，不将现有登录会话检查等同于完整登录表单验收。
- 签名维护任务 80bd93ab-a138-9b8f-9c81-ffe92c05b11d 已真实执行成功。定时触发器 pbp8s3ytna 已创建，每 15 分钟一次、无重试；首次定时任务 timer-dcd91678-71af-42c7-9101-f2bb6176dbbb 返回 403 Forbidden。定位到 worker 命令验签拒绝，具体消息封装或请求解析原因尚未确认，不能声称自动维护已通过。
- 后端 108 项及前端 16 脚本／类型检查通过；真实云端 AI 调用、跨浏览器作品保存与恢复尚未验收。当前仅适合邀请测试，不代表正式上线验收完成。
- 部署分片及早期合成验证残留未清理；不删除完整源包，不做广泛桶清理。
