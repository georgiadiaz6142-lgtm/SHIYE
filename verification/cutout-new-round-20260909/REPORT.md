# 自动抠图完成后开启新一轮 · 2026-09-09

已修复收藏完成后再次进入仍显示上一轮结束页的问题。原因是保存成功只关闭了界面，预览状态仍留在内存，后端最近会话也会被无条件恢复。

保存成功（包括“收藏并用于创作”）后重置本轮任务、预览、选框与临时 URL。重新进入时，用已持久保存的收藏来源识别已完成会话，不再恢复该会话；此前已收藏的旧任务也适用。完成判断基于已保存工作区，不以尚未保存的内存资产判断。未完成任务仍可恢复。

[run-03 浏览器结果](./run-03/results.json)：1440px 桌面、390px 模拟手机均通过。覆盖收藏后立即重新进入、刷新后进入、第二轮上传、未完成任务导航／刷新恢复、收藏并用于创作后重新进入；同时回归修边、白边、收藏、入书和刷新恢复。每个宽度只为两轮主动上传各调用一次模拟供应商，进入工坊和恢复不会增加调用。

已检查[手机新一轮截图](./run-03/new-round-390.png)。run-01/02 的失败是测试脚本定位到重复或隐藏的导航入口；使用实际可见入口及阅读页收起按钮后通过，历史结果保留。没有调用真实百度或上传个人照片。

JS 语法、`npm run check:prototype`（10 项合并检查）、`git diff --check` 通过。此轮仅修改工坊状态和收藏完成判断；自动命名仍处于讨论阶段。保留上一轮已授权移除的“修正只在本机进行，不调用百度”提示改动。
