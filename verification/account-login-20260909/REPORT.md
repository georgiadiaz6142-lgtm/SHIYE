# 首页账号密码登录验收 · 2026-09-09

## 已完成

- 首页“登录/注册”新增“账号密码登录”，输入现有账号与密码直接进入“我的书架”。默认邀请码入口保留，手机号登录／注册继续禁用。
- 首页与 `/admin` 专用入口使用同一个服务端认证处理器和登录尝试次数限制，均无需先验证邀请码。客户端不能指定账号 ID 或角色。
- 管理员账号持久保存固定 UUID 和角色。会话记录绑定账号 ID，权限由服务端校验；用户名修改和服务重启不改变 ID。不同浏览器登录拥有各自会话，对应同一个账号。
- 现有管理员数据首次启动时补齐 ID／角色，后续启动保持不变；新建实例初始化即具有 ID。
- 登录错误有反馈；密码不写入浏览器存储，关闭弹窗、切换方式或成功登录后清空表单内容。
- 管理员入口显示及跨标签身份切换沿用前轮修复，普通受邀用户不能访问管理接口。

## 验证

- `backend.txt`：35 项后端测试全部通过，包含原权限隔离回归、新旧账号迁移、改名／重启后 ID 保持、不同浏览器无邀请码登录、专用路径与首页路径对应同一账号、角色伪造拒绝和单会话退出。
- `browser.cjs`、`run-01/results.json`：1440、390、320 宽度全部通过。完整操作首页账号登录、错误密码、关闭清空密码、自动进入书架、第二个独立浏览器登录、普通邀请码入口、管理工具及原专用登录页。无页面运行错误，窄屏表单无横向溢出。
- `run-01/account-form-*.png` 与 `admin-shelf-*.png`：隔离测试数据的表单及登录后书架截图。
- `live.cjs`、`live-results.json`：原 4176 服务新入口可用、旧入口保留、匿名管理接口拒绝、账号固定 ID 已生成。

## 本机更新与边界

升级前原账号配置及邀请码文件备份在 `.local/account-login-backup-20260909/`，均不进入 Git。账号名称仍为“宋静雯”；逐项核对密码散列、盐、API 配置及邀请码文件保持不变。原本机服务已更新。

用户此前自行修改过密码，本轮没有使用失效初始密码重试，也没有重置密码；真实账号成功登录未自动复验，完整认证流程使用隔离测试账号验证。

当前仍为 `127.0.0.1:4176` 本机服务。同一台电脑不同浏览器可以登录相同管理员账号，跨设备访问需要后续部署共同可访问的服务。普通手机号注册、跨浏览器作品云同步和公网部署不属于本轮；未执行百度调用。
