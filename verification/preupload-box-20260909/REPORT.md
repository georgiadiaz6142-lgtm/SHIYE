# 上传前预览与框选验收 · 2026-09-09

已完成 Song 要求的四项调整：选图后显示本机预览；去掉发送百度的确认复选框；上传按钮上方增加“框选具体景物”；结果页取消重新框选入口，保留手动修边。

选择和框选不会发送照片。点击“上传并自动抠图”直接处理整张；点击“上传并框选抠图”直接提交照片及归一化选框，不先调用一次自动抠图。一次结果仍是一张前景贴纸，框选效果取决于百度返回的蒙版。

## 实际配置

- Song 已明确确认“持续解除本机次数和期限限制”。`.env.local` 设置 `SHIYE_BAIDU_LIMITS_DISABLED=true`，未记录或提交密钥；原次数与截止时间配置被显式开关绕过。
- 原真实服务已在 `127.0.0.1:4176` 重启。健康接口返回 `status=ok`、`limitsDisabled=true`、`liveAvailable=true`、`localTtlSeconds=3600`。
- 原运行目录与调用记录保留。改动前累计 10 次，收尾读到 11 次、无排队或运行任务。期间新增记录的发起来源未核实；本轮开发测试均使用隔离模拟供应商，没有由测试脚本发出真实百度请求。
- 解除本机次数／期限限制不改变供应商计费、并发保护、任务幂等、会话隔离、文件校验和临时图片有效期。

## 验证证据

- [后端测试](./backend-tests.txt)：21 项通过，包括无限制模式下超过 10 次仍可执行并持续记账、保留有限 TTL、第一项任务直接使用选框；有界模式与隔离检查仍通过。
- [上传前浏览器结果](./run-02/results.json)：1440px 桌面与 390px 模拟手机通过。确认选图及框选时零 POST；空选框在本机阻断；鼠标和触摸坐标正确；提交只有一次 refine，没有前置自动请求；结果页不再出现框选入口；无横向溢出与页面脚本错误。脚本还检查 EXIF 方向、更换照片、取消框选、损坏和超过 10 MB 的图片不上传。
- [桌面预览](./run-02/preview-1440.png)、[手机框选](./run-02/box-390.png)、[桌面结果](run-01/result-1440.png)。预览与框选截图已人工检查。
- [修边与保存回归](./retouch-01/results.json)：桌面和模拟手机通过自动处理、恢复原像素、擦除、撤销重做、缩放平移、对比、保存失败恢复、修正暂存、白边、收藏、入书及刷新重开。修边没有额外供应商调用。
- `npm run check:prototype`：语法检查及 10 项合并检查通过；`node --check shiye-editorial-prototype/segmentation.js` 与 `git diff --check` 通过。

第一次截图发现空框错误提示在画框后残留，已修复并以 run-02 复验。run-01 保留为过程记录。

## 范围与限制

本轮验证交互和调用链路，不证明复杂风景的真实抠图质量提升；此前的真实质量结论仍有效。超过 2400 万像素的大图后端适配仍是后续事项。文字输入、字体、书架和双页编辑逻辑未改。

工作分支 `feat/baidu-local-retouch`；改动前节点 `b14532d`，标记 `checkpoint/before-preupload-box-20260909`。真实配置文件不进入 Git。
