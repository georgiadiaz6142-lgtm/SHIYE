# 软纸翻页同步 · 2026-09-08

## 结果与范围

Song 已认可首页软纸自然度，本轮按授权同步至手帐翻阅和编辑前后页。地址仍为 http://127.0.0.1:4176/，原目录 `shiye-editorial-prototype`。

- `app.js`：将认可的 40 段软纸弧面设为唯一共享 `drawSideTurn`，删除旧 16 段硬板式近似；首页调用迁入同一函数。翻阅/编辑沿用原正反面内容映射，时长与首页统一为完整翻页 1450ms；减少动态效果时仍直接切页。
- `styles.css`：让首页的 2400px 透视同样适用于其他翻页层；边缘接缝处理、纸面明暗和投影直接复用。
- 首页文字、素材、HTML、存储逻辑、手帐编辑内容均未改变。未新增产品目录、依赖、模型或纸样/画笔。本轮不重做静止书本的结构，不改封面/封底或缩略图直接跳页规则。

## 验证

浏览器技能要求实际检查。本机未安装 agent-browser CLI，使用已有 Playwright/隔离 Chrome，无下载/安装，不连接用户浏览器资料。测试中的作品只存在于隔离上下文。

最终 `softbook-sync-03/results.json`：8/8 通过，无页面 JS 异常。覆盖首页前后翻、双页侧拖和短拖取消、真实背面、编辑翻页后继续编辑、照片裁切/收藏既有流程、手机模拟触控软纸前后翻、减少动态效果。检查手帐页面内容未变化。4176 返回的 HTML/JS/CSS 字节与本地一致。

`softbook-sync-guards-01/results.json`：2/2 通过。编辑中改变窗口取消翻页且解锁；保存失败则保留当前页、不开始动画。

`merge-unit.cjs`：10/10 通过，既有数据合并边界回归。

视觉证据：已查看桌面 `softbook-sync-01/book-side-backface.png`、`editing-side-turn.png` 和最终 `softbook-sync-03/mobile-soft-middle.png`。这些运行的产品代码哈希相同。

调试记录保留：01 原有 7 项通过；02 补手机触控检查通过，但既有照片测试在异步解码完成前立即计数产生失败，失败截图显示照片随后正常进入选区。仅为测试脚本增加元素就绪等待，产品照片代码没改；03 最终 8 项通过。

未验证真机 Safari、低性能设备和大量复杂元素的性能。本轮不代表真实 AI/后端技术关口通过。

## 恢复节点与最终哈希

恢复节点：`archive/2026-09-08-before-softbook-sync.tar.gz`，包含修改前 app.js、styles.css、PROJECT.md 及旧回归脚本，已核对归档清单；不包含用户浏览器作品。恢复前需检查后续修改，不直接覆盖。

| 文件 | SHA-256 |
|---|---|
| app.js | `91516895d5c00f02e50cdb89b759009cf3187d2f19c16ad6f53988d4b2857ef2` |
| styles.css | `1ae4e10f14e6953a898d7879998745350ec4f53acb7bb632f4a90dfe826d36c8` |
| index.html（未改） | `ff278abca8ecf5a5c76546705b0334d56e2593412fdc3b753d321723bdaf9ea2` |
