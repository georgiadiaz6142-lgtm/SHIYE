# 12 种纸样交付与验证 · 2026-09-08

## 目标与范围

- 目标为 `http://127.0.0.1:4176/` 的拾页米白首页对应应用，目录 `/Users/song/Documents/拾页/shiye-editorial-prototype`。测试逐字节核对服务返回的 app.js、styles.css、index.html 与本地文件一致。
- 产品修改仅 app.js、styles.css；增加纸样定义、三组选纸小样、当前页换纸的校验/滚动与焦点保留。index.html、工坊小剪刀、软纸翻页逻辑和存储结构未修改。
- 基础书写：奶油空白、细点纸、方格纸、横线纸。
- 纸张材质：棉纸、水彩纸、浅牛皮纸、旧信纸。
- 轻装饰：植物边饰、邮戳信笺、淡拼贴、相册边框。
- 新纹理使用本地 CSS 与内嵌 SVG 绘制，无下载素材、安装依赖、模型调用或照片上传。是纸感样式，不是扫描纸材。
- 换纸仅改变当前页背景，内容和其他页不变；沿用撤销、重做、IndexedDB 本机保存。只读翻阅不提供选纸入口。基础小样增强点线可读性，实际页面仍保持原有淡点线。

## 恢复节点

`/Users/song/Documents/拾页/archive/2026-09-08-before-paper-library.tar.gz`，包含修改前 app.js、styles.css、PROJECT.md。已核对归档列表。恢复仅应按用户明确授权执行；不要覆盖期间新增的其他改动。该归档不包含浏览器作品，回退代码不会自动回退作品的纸样字段。

## 验证与结果

按浏览器验证技能要求，使用本机已有 Playwright 和隔离 Chrome。没有使用用户浏览器上下文，测试拦截外部请求。

最终专项脚本：`paper-library.cjs`，证据：`paper-library-03/results.json`，4 组全部通过，无页面 JavaScript 错误：

1. 全部 12 种可选，选中状态唯一；逐种保存后核对贴纸/文字完整数据与另一页不变、缩略图类名正确；撤销与重做；刷新、从书架重新打开后内容和纸样保留。
2. 不同纸样绑定到翻页正反面，翻阅模式不出现选纸面板。
3. 390×844 模拟触摸设备可选择全部 8 种新增纸样，无横向溢出；关闭面板后可进入单页翻阅并保留纸样。
4. 注入存储配额失败后明确提示并阻止进入翻阅，内容仍在；恢复存储后重试成功。

本轮回归：`paper-regression-01/results.json`，8 组通过，包含首页侧边拖动、翻阅取消/背面、编辑软翻、小剪刀自由圈选与透明输出、分段点选和撤销、框选、模拟手机软翻与小剪刀。此回归后仅调整基础纸样小样的缩放/点线可读性，最终专项已覆盖该调整。

`node --check app.js` 通过；`merge-unit.cjs` 的 10 项数据合并单测通过。

首轮 `paper-library-01` 的手机翻阅断言过早，未等待异步保存及模式切换完成；调整测试等待 `.read-mode` 后，02 和 03 均 4 组通过。没有为此修改产品保存流程。01/02 为过程证据，最终以 03 为准。

已查看桌面选纸面板、材质与装饰纸面、两种纸样翻页中间帧、手机选纸与页面截图。真实手机、Safari、低性能设备未测试，审美满意度仍由 Song 判断。本轮不验证真实 AI 自动多主体提取，也不代表产品可以上线。

## 最终文件 SHA-256

- app.js：`257b83e816ff5e7c3f345fdc3781df0f4aa7a26e9cd8fa7b66d896349a52802e`
- styles.css：`cd1d8bbc10da7b90054451a1c3ec0a68161f760ba9a61c66bd78817078e260e3`
- index.html（未改）：`ff278abca8ecf5a5c76546705b0334d56e2593412fdc3b753d321723bdaf9ea2`
