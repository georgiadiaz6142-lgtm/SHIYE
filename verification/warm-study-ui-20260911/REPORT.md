# 暖光书房 UI 换色验证

2026-09-11。Song 授权先保存节点、创建分支，再调整颜色和特效；所有排布与操作形式保持不变。

## 恢复节点与范围

- 开发前节点：`archive/20260911-175002-before-warm-study-ui/`，1881 个文件逐项核验。包含当时未提交代码；不包含密钥、私人运行数据或用户作品。
- 当前分支：`feat/warm-study-ui-20260911`。归档是恢复节点，未把工作区改动提交为 Git commit。
- 产品文件：`shiye-editorial-prototype/styles.css` 末尾追加独立视觉样式；`app.js` 仅为贴纸卡片增加经过转义的 `data-category` 属性，供分类底色使用。
- 燕麦背景、象牙白面板、咖啡色主按钮、苔绿选中态；静态暖光与轻纸纹、柔和阴影、短暂淡入和悬停反馈。正常动效设置下保留原有贴纸悬停放大；尊重系统减少动态效果设置。
- 原 CSS 前缀未改写。未修改布局尺寸、字体、响应式断点、翻页逻辑、作品数据、首页场景资源或 API 配置。

## 已验证

- `npm run check:frontend`：15 个脚本语法和入口依赖检查、前端 TypeScript 检查通过。
- `git diff --check`：本轮产品文件通过。
- `browser.cjs`：隔离临时服务、虚构账号与独立 Chrome 上下文，31 项通过，无页面 JavaScript 错误；未使用真实账号或调用外部 AI。
- 29 组前后对比：1440、800、390 宽度下的书架、贴纸仓库、工坊、编辑器、文字/贴纸/纸张/页面面板、封面编辑，另加桌面双页编辑和翻阅。比较所选可见控件坐标、尺寸、字体族/字号/字重，全部一致；未产生横向页面溢出；书籍数据一致。
- 封面和书页可见内部像素对比通过，RGB 通道最大差为 1/255（浏览器图片重新栅格化误差）。排除透明圆角、视口外区域、手机固定导航遮挡，以及覆盖在作品上的翻页按钮；这些区域的界面颜色本来就属于本轮调整范围。
- 正常设置下贴纸悬停放大保留；减少动态效果设置下相关过渡关闭。
- 已人工查看桌面书架、贴纸仓库、编辑器、半屏与手机文字面板截图。
- 4176 的公开 `styles.css` 和 `app.js` 均返回 200，内容哈希与工作区一致。

## 证据与边界

- [机器结果](results.json)、[验证脚本](browser.cjs)。最终有效结果为 31 项通过；`failure.png`、`art-before.png`、`art-after.png` 是前期截图范围/抗锯齿诊断产物，不代表最终失败。
- [书架](shelf-1440-after.png)、[贴纸仓库](collection-1440-after.png)、[桌面编辑](text-1440-after.png)、[半屏编辑](text-800-after.png)、[手机编辑](text-390-after.png)。相邻 `*-before.png` 为同一 DOM 下禁用新样式的基线。
- 本轮为视觉调整验证，未重新验收完整账号、同步、自动抠图或翻页动画链路。真实手机、Safari 和 VS Code 集成浏览器尚未验证；未公网部署。
