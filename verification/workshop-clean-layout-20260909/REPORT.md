# 工坊界面精简 · 2026-09-09

修改前节点：`712a63f`。回退标记：`checkpoint/before-workshop-layout-20260909`。新分支：`feat/workshop-clean-layout`。创建分支前已确认 tracked 工作区干净。

## 已实现

- 真实百度工坊删除重复的“贴纸工坊”大标题、副标题和能力说明栏；保留顶部面包屑与“返回上一步”。
- 默认不展示图片格式、大小和整张／框选说明。格式不支持、文件无法读取或超过 10 MB 时才出现错误提示。框选模式仅提供一句拖动提示。
- 删除占据右栏的大标题和介绍，必要操作说明放入图片区下方默认折叠的“操作说明”。
- 上传、处理结果采用单列，照片按比例显示并限制高度，给操作按钮留出空间。
- 百度返回后，同一主图区直接展示透明 PNG，原图不再同时显示，也不再在下面另放一个小候选卡。棋盘底用于显示透明部分；原图仍保留供手动修边使用。
- 结果下方左侧“手动修边”，右侧“确认透明贴纸”。删除结果页“选择照片”按钮，顶部返回上一步可以回到选图。修边应用后主图区展示修正结果。

## 验证

- [浏览器结果](./run-01/results.json)：1440px 桌面、390px 与 320px 模拟手机通过，无页面脚本错误及横向溢出。测试断言标题／常驻说明／右栏不存在，说明默认折叠，照片预览与框选不发请求，直接框选仅提交一次任务；结果页不再显示原图、小候选列表或选择照片按钮。两按钮左右并排，900px 视口高度中在首屏可见。
- 返回选图、EXIF 旋转、更换照片、取消框选、无效图片和超过 10 MB 图片阻断均通过。
- [修边回归](./retouch-01/results.json)：1440px 与 390px 通过恢复、擦除、撤销重做、对比、缩放、修正暂存、保存失败恢复、透明预览、白边、收藏、入书及刷新重开。修边没有增加模拟供应商调用。
- 已查看 [桌面结果](./run-01/result-1440.png)、[手机选图](./run-01/preview-390.png)、[窄屏结果](./run-01/result-320.png)。
- `node --check shiye-editorial-prototype/segmentation.js`、`npm run check:prototype`（含 10 项合并检查）、`git diff --check` 通过。

所有浏览器验证使用隔离的本机模拟供应商和合成素材，没有上传个人照片或调用真实百度，不代表百度真实抠图质量验收。业务修改限于 `segmentation.js` 和 `segmentation.css`；后端、真实配置、文字输入和字体没有改动。历史验收文件原样保留。
