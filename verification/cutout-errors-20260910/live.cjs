const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');
const {chromium}=require('/Users/song/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const base='http://127.0.0.1:4176',browser=await chromium.launch({channel:'chrome',headless:true});try{const ctx=await browser.newContext();await ctx.route('**/*',r=>new URL(r.request().url()).origin===base?r.continue():r.abort());const p=await ctx.newPage(),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto(base,{waitUntil:'networkidle'});await p.locator('[data-action="access-open"]').waitFor();for(const file of ['segmentation.js','segmentation.css'])assert.equal(await(await ctx.request.get(base+'/'+file)).text(),fs.readFileSync('shiye-editorial-prototype/'+file,'utf8'));const health=await(await ctx.request.get(base+'/api/health')).json();assert.equal(health.mode,'live');assert.equal(health.limitsDisabled,true);assert.equal((await ctx.request.get(base+'/api/admin/overview')).status(),403);assert.deepEqual(errors,[]);const result={status:'PASS',scope:'current local service entry and exact updated static files',limitsDisabled:true,adminAccessStillProtected:true,realBaiduCalls:0};fs.writeFileSync(path.join(__dirname,'live-results.json'),JSON.stringify(result,null,2));console.log(JSON.stringify(result));}finally{await browser.close();}})().catch(e=>{console.error(e.message);process.exitCode=1;});
