# TOS 专用桶创建与真实读写验证

2026-09-11，Song 明确允许创建已说明配置与计费方式的专用桶，并进行小规模合成数据验证。

## 已完成

- 创建 `shiye-private-20260911-c7f2`：北京、私有、标准存储、单 AZ。未开启版本控制、分层命名空间、日志分析或额外加速服务，未购买资源包。
- 官方控制台上传唯一合成文件 `shiye-verification-565cb45a-5a71-440f-b60b-51959493f4ed.json`，73 字节；上传任务显示成功 1、失败 0。
- 通过控制台生成的签名下载请求读取对象，HTTP 200，逐字节比较与上传内容完全一致。SHA-256：`3d2f1b03a7b37cf232cddd5e1903afd3bac64d96d420f33862ec6b61eece42fe`。
- 移除签名查询参数后访问相同对象，HTTP 403，确认匿名读取被拒绝。签名链接仅在内存使用，未写入报告或输出。
- 删除且仅删除上述测试文件；删除任务显示成功 1、失败 0。重新加载桶文件列表后显示“暂无数据”“已选择 0 / 0个”。桶保留供后续接入。

## 可复核工具记录

Tabbit 任务 `shiye-deploy-cost`：读取校验 `tos-real-read-hash`；删除目标检查 `tos-cleanup-target`、`tos-cleanup-inspect-final`；删除操作 `tos-cleanup-test-execute`；删除成功 `tos-cleanup-test-verified`；刷新确认空桶 `tos-empty-bucket-refresh`。

## 验证边界与下一步

这是实际 TOS 控制台上传、下载与匿名权限验证，不是应用 SDK 并发条件写入验收。`server/tos-smoke.ts` 尚未连接真实桶执行；应用专用权限与凭据仍待接入。此前 98 项后端测试是本地验证，本轮未改代码，未重复运行。

未创建数据库、应用、函数或 Timer，未发布网站，未修改已有网关、其他项目桶及路由，未迁移用户数据。云端入口目前仍为 cloud-test、真实 AI 关闭，不能作为正式上线完成。

接下来为后端配置仅访问此桶的专用权限，完成 SDK 条件写入、并发冲突与重启恢复验证，再进行云端应用部署与端到端验收。正式用户进入前还需落实数据备份。
