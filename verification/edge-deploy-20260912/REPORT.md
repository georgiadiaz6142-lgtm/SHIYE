# 自动贴边差异部署 · 2026-09-12

Song 试用本地后明确授权发布。原应用 `862432459a9a`／网页函数 `ulvzu4v3` 已发布稳定版本 **10**，替代版本 9，100% 流量；发布记录 `s3s3tfjl4c98soup`，平台状态 `done`。

入口：https://sm994o19l0g1rajv9qfqf.apigateway-cn-beijing.volceapi.com/

## 已发布与验证

- 从线上拉取原完整包，逐文件比对后仅替换 7 个运行文件：3 个贴边前端文件、`dist/server/app.js` 及其 source map、新增压缩中间件及其 source map。前端仅这 3 个文件与线上不同。服务端入口差异只有导入和注册中间件两处。具体指纹见 [changes.json](changes.json)。
- 全部 2510 个文件的路径／内容指纹与本地待发布清单一致。云端生成包 170,160,029 字节，SHA256 `b104e0c63fa33ca5ed558ed3adca68eaeba06045add8ff68e64ed791f0efd5dd`；只在独立临时目录生成，没有编辑正在服务的代码文件。
- 提交代码前后核对运行命令、端口、环境变量、CPU／内存、角色、请求设置及存储／网络配置的摘要一致。随后以原应用 `--skipbuild` 发布。未迁移账号或作品、未更新 worker、未调整实例配额。
- [公网 Chrome 检查](live-verification.json)通过：3 个公开文件 HTTP 200 且 SHA256 与本地一致；真实 Worker 两次初始化、共 4 次合成图像计算全部成功。同一个 Worker 连续两次计算，仅初始化一次。
- 本次公网实测首次工具准备 5.003 秒；4 次算法计算为 373／300／366／303 毫秒。第二个 Worker 准备 1.286 秒。这是本机 Chrome 和合成图像的测量，不代表所有设备或个人照片耗时。
- 公网 OpenCV 确认 gzip、3,526,614 字节及一年 immutable 内容版本缓存，约减少 68% 传输。页面交互、取消重试、桌面右侧／手机底部布局及保存恢复沿用[本地 8 组验证](../edge-loading-20260912/REPORT.md)。本轮公网未登录或上传用户照片；隔离 Chrome 已关闭，本机 4176 预览继续保留。

## 未确认的问题

- 手账翻页 `app.js`、主样式 `styles.css` 和首页 3D 书本 `book.js` 在部署前就与本地逐字节一致，见 [比对证据](unchanged-book-comparison.json)。不能把“线上翻页僵硬”归因于漏发这些文件；本轮没有修改翻页算法，也未确认用户设备上的触发原因。
- “贴纸制作的边框在外边”所指是选区偏移、白色描边还是工具栏位置，已向 Song 询问，尚未获得明确回复。已上线桌面照片右侧工具栏，不能据此声称其他边框问题已解决。
- 真实微信／Safari、原先首页照片和日历黑块仍未验收。后台 Timer 的既有 403 未在本轮处理。

## 发布过程与保留内容

本机官方大包 PUT 在 240 秒后超时，只传输约 7.7 MB。随后改在原实例临时目录校验并组装差异包，从云端上传。中途处理了清单排序差异、正式暂存源与普通上传地址的区别、实例自动回收及 CLI 参数校验；这些失败均未作为发布成功依据。最终通过 CLI 实际使用的 `GenTempTosObjectUrl` 正式暂存、`UpdateFunction` 和原应用发布流程完成。

运行配置中的身份数据和密钥未写入包或本报告。未删除旧版本、私有桶历史源包或暂存对象。原稳定版本 9 和既有发布记录保留；如需回退应针对该版本按明确授权操作，不能覆盖后来版本。
