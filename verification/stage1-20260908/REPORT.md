# 第一阶段开发记录 · 2026-09-08

> 此文保留首批源码实施时的历史结果。后续依赖安装、类型检查及真实本机 HTTP 验证已完成，最新状态见 [HTTP-INTEGRATION.md](./HTTP-INTEGRATION.md)。

## 首批实施时结论

Song 已授权进入开发。开发前已完整阅读《拾页-技术适配声明》和《拾页-第一阶段技术开发文档》。首批源码已写入，前端接口模拟及现有原型回归通过；**项目依赖尚未获准安装，后端未编译、未启动、未联调，真实模型未接入，第一阶段没有完成。**

当前 4176 仍是原 Python 静态预览进程；没有替换服务或迁移浏览器 origin。新增“验证模拟任务流程（非 AI）”入口在真实浏览器中需要后端才可运行，本轮测试使用了隔离浏览器内的 API 拦截模拟，不能作为后端运行证据。无后端时页面明确提示连接不可用，本地小剪刀仍可使用。

## 实际变更

- `package.json`、`tsconfig.json`、`.gitignore`、`.env.example`：一套 TypeScript 工程清单及非秘密配置，未安装依赖或初始化 Git。
- `server/` 与 `shared/`：单一 Express 后端源码、Zod 请求/存储校验、任务提交/查询/取消/恢复、模拟图及独立蒙版合成、受控静态文件/媒体访问、13 项后端测试。
- `shiye-editorial-prototype/segmentation.js`、`segmentation.css`：原工坊内的明确模拟入口、候选选择、圈选及点提示、透明 PNG 预览、每枚白边、收藏和回填。
- 原 `app.js` / `index.html`：接入新模块；使用原 IndexedDB `data` store 的独立 `blob:<assetId>` 键与工作区元数据同事务写入，不升级数据库版本。旧 data URL 保持可读。
- PROJECT 与两份阶段文档：同步开发授权、实际进度与尚未运行的验证边界。正式 PRD、产品设计文档、原 AI 与后端技术方案未修改。

原 `styles.css` 保持哈希 `cd1d8bbc10da7b90054451a1c3ec0a68161f760ba9a61c66bd78817078e260e3`。首页、绿色书本和旧视觉素材保留，不恢复木桌版或画笔试验。新增 CSS 仅针对模拟工坊类名。

## 恢复节点与数据保护

开发前归档：[2026-09-08-before-stage1-161858.tar.gz](../../archive/2026-09-08-before-stage1-161858.tar.gz)。包含原 app.js、styles.css、index.html、原型 README、PROJECT 及两份阶段文档，7 个文件均与归档前源文件逐项核对。

归档不包含浏览器作品，也不包含后续新增源码。未删除、移动或覆盖旧归档；没有操作 Song 的浏览器上下文。所有浏览器测试使用独立临时上下文；测试生成的作品和 Blob 不进入 Song 的浏览器作品库。

## 已执行验证

| 验证 | 结果 | 证据与限制 |
| --- | --- | --- |
| app.js / segmentation.js 语法 | 通过 | `node --check`；不是完整前端业务验收 |
| 7 个 TypeScript 文件语法 | 通过 | Node `--experimental-transform-types --check`；只查语法，不查类型、依赖解析或运行 |
| 原合并单测 | 10/10 通过 | `verification/cream-20260908/merge-unit.cjs` |
| 纸样与原数据回归 | 4/4 通过 | [纸样结果](../cream-20260908/stage1-paper-baseline-01/results.json)：12 纸样、撤销/保存/重开、翻页正反面、模拟移动及保存失败保护 |
| 小剪刀与侧边软翻 | 8/8 通过 | [剪刀/翻页结果](../cream-20260908/stage1-scissors-regression-01/results.json)：本地轮廓、分段点选、矩形、首页/阅读/编辑软翻及模拟触摸 |
| 新工坊前端 API 模拟 | 3/3 通过 | [最终模拟结果](./frontend-mock-04/results.json)：候选补提/纠错、独立白边、Blob 收藏重开、失败重试、原页回填、模拟移动；无页面错误或外部请求 |

已查看首页、模拟候选、透明预览及模拟移动截图。接口模拟测试中的候选图是测试替身，不用于分割效果或候选外观质量验收。真实后端的合成图与蒙版代码仍待运行。

`frontend-mock-01/02/03` 是过程证据：首轮使用了首页不存在的侧栏入口，第二轮选择器不唯一，第三轮刷新后未先从首页进入侧栏页面；修正的是测试导航定位。最终以 `frontend-mock-04` 为准；没有为通过这些导航断言改写产品页面。

源文件指纹：[source-hashes.json](./source-hashes.json)。该清单记录首批代码，不含后续状态文档文本。

## 尚未执行与限制

- 未安装 package.json 中的依赖，没有 node_modules 或 package-lock.json；完整 TypeScript 类型检查与 13 项后端测试未运行，源码仍可能有待联调发现的问题。
- 未启动 Node 服务，未验证真实 HTTP、实际磁盘任务恢复或后端图片处理。浏览器内模拟接口成功不等于这些能力通过。
- 当前后端源码只提供合成测试图模式；个人照片上传返回拒绝，live 配置不允许启动。真实 fal 适配器尚未实施，无供应商调用、费用或照片传输。
- 真实多主体发现、轮廓转提示与贴边质量、50 张样本、P95、成本、供应商完整图片政策、真机/Safari 尚未验收。
- 新模拟圈选与点提示用于验证契约和界面；原小剪刀本地裁切仍独立标明，不作为真实模型兜底。

## 下一步

依赖申请已提交：项目内安装 Express 5.2.1、Zod 4.3.6、Sharp 0.34.5、TypeScript 5.9.3、@types/express 5.0.6、@types/node 22.19.15 及必要传递依赖，生成锁文件，不全局安装或升级 Node。

收到批准后安装并核验实际解析版本，执行完整类型检查与后端测试，修复问题后进行真实本机 HTTP 联调。4176 的现有静态服务归属与切换影响须在替换前明确处理，保留原 origin 和作品。真实模型、照片、费用及部署继续遵守独立确认边界。
