# 拾页 SHIYE Product Design Specification

> 中文名称：拾页（暂定，尚未最终确认）  
> 英文名称：SHIYE（暂定，尚未最终确认）  
> 文档版本：V1.5  
> 文档状态：首发 Web 产品设计基线  
> 更新日期：2026-09-08  
> 适用对象：产品设计、交互设计、视觉设计、前端开发、测试与项目负责人。

## 0. 文档说明

### 0.1 产品摘要

【已确认/页面事实】拾页是一款面向中国大陆用户、同时适配手机与电脑浏览器的轻量、私密、local-first 电子手帐产品。用户把照片中的一个或多个主体分别制作成带白色描边的贴纸，收藏到个人贴纸仓库，再通过选书、取书、开书、逐页创作、自动保存和归架完成一段有仪式感的创作旅程。游客可先本地制作；登录用户可跨设备同步，并使用按权益开放的 AI 能力。

【已确认/页面事实】产品不是普通图片编辑器；页面应由可编辑贴纸、文字、纸张和结构化数据组成，不把整页合成为不可编辑图片。

### 0.2 标记规则

- 【已确认/页面事实】：来自产品负责人确认或现有原型可观察事实。
- 【合理推断】：为使已确认体验闭环而推导出的逻辑。
- 【建议设计】：设计团队建议采用、但仍可调整的方案。
- 【未知/尚未确认】：需要产品负责人、视觉或技术进一步决定。

### 0.3 设计边界

【已确认/页面事实】现有 index.html、styles.css、app.js 和 README.md 仅作为产品结构、业务逻辑与交互流程参考，不作为正式视觉基线。正式产品需要重新建立 UI 视觉系统，不直接沿用现有原型的界面造型、配色、组件样式和页面排版。

【已确认/页面事实】首发包含邀请码、手机号登录、云同步、会员兑换码、AI 文案、会员 AI 排版和管理员后台；不设计在线支付、自动续费、社区、公开分享、多人协作、实体打印、AI 整本生成、AI 重绘贴纸、复杂滤镜或真实 3D 书本。

### 0.4 本轮确认同步（2026-09-07）

【已确认/页面事实】Song 已确认：已有手帐默认先翻阅，主动点击“编辑”才展开工具；首发不增加自动分类、自定义贴纸包、绘画画笔和用户作品导出；注册后一句话选主体同图继承已用、新图按账号每图额度，AI 文案仍按月衔接；退出登录保留账号本地作品，但再次登录同一账号前不可访问。本文已将这些决定同步至对应交互、异常提示及验收条目；文档确认不代表原型或正式代码已经实现。

---

### 0.5 2026-09-08 已确认的交互补充

以下更新取代早期“不做双指 / 不做轮廓纠错”的约束；不改变米白首页视觉、不引入木桌版本。

1. 上传后先自动发现所有主要物体；用户选择结果。对漏提、错提对象，在原图画大致轮廓，显示半透明提示区域，提交真实交互分割后自动贴合物体边缘。新结果须确认；可撤销或重画提示。不能将用户画的多边形直接裁切后称为 AI 自动贴边。
2. 收藏成功后提供“进入创作”；预览同时提供“收藏”和“收藏并用于创作”。编辑器进入工坊时记住 bookId/pageId；保存资产成功才回原页。来源页面不存在时改为选书，不擅自放入其他页面。首页来源选择已有书或新建；取消保留已收藏资产。
3. 选中贴纸出现低饱和柔光与细边框；四角控制柄等比缩放，上方独立圆柄旋转。双指以两点距离调整大小、夹角调整旋转；保留加减与旋转按钮。一次手势一次撤销，不连续写盘；手势与仓库滚动、翻页分开。
4. 文字编辑框提供横排与竖排；竖排从上往下、列从右往左，采用真实文字排布；方向与尺寸随页面持久保存，不将整段横排字旋转冒充竖排。
5. 桌面翻阅是真实双页：内封＋第 1 页，第 2＋3 页，第 4＋5 页……；同张纸正反面是相邻页面，背面内容不镜像、不重复正面。手机竖屏按页阅读；封面与封底可打开。底部页角拖动不足回弹，越阈值提交；减少动态时直接切页。
6. 编辑始终聚焦单页，页面元素不跨书脊；翻阅不改元素。点击页面空白或缩略图选择要编辑的一页，随后主动点击“编辑”。

【未知/尚未确认】供应商的多主体召回和轮廓提示支持能力须真实技术验证；当前静态原型只有本地矩形/轮廓裁切，界面须明确“不会自动贴边”，不展示虚假的 AI 成功状态。

## 1. 产品设计原则

### 1.1 一本书，不是一个文件

【已确认/页面事实】所有核心文案、转场和容器造型都应强化“书、纸张、贴纸和私人书架”，避免文件管理器或企业后台感。

### 1.2 仪式感服务于记忆，不阻碍编辑

【已确认/页面事实】取书、开书、翻页和归架是品牌体验，但必须可跳过、可降级，并在进入编辑后保持操作直接高效。

### 1.3 保持可编辑

【已确认/页面事实】贴纸、文字、纸张、页面和层级均独立存在。视觉上可以像手作拼贴，底层不能变成一张不可编辑合成图。

### 1.4 私密和可理解

【已确认/页面事实】不引导公开发布。调用第三方 AI/抠图前说明输入范围；游客明确提示数据仅在当前浏览器，登录用户明确区分“已保存到本机”和“已同步”。完整原图不作为长期云端资产。

### 1.5 轻量而克制

【已确认/页面事实】不使用高成本真实 3D、不堆叠复杂装饰、不把所有功能挤在单屏。每个阶段只呈现当前任务所需的主要动作。

## 2. 品牌气质和视觉关键词

### 2.1 品牌气质

【已确认/页面事实】温暖、安静、私密、手作、旅行记忆、纸张收藏、略带怀旧但不陈旧。

### 2.2 视觉关键词

- 私人书桌
- 暖色纸张
- 克制的封面画廊与书脊语言
- 墨绿色
- 珊瑚红或苔藓绿点缀
- 纸张纤维与轻微颗粒
- 白边贴纸
- 胶带、邮戳、标签和手写标记
- 编辑出版感宋体与清晰无衬线字体组合

### 2.3 应避免

【已确认/页面事实】避免企业 SaaS 仪表盘、大面积高饱和渐变、过度卡通、装饰过密、真实 3D 建模、仿真木板书架和拥挤单屏编辑器。避免把社区广场、签到任务、素材商城或儿童化“咕卡”语言带入核心体验。

### 2.4 正式界面设计方向

【建议设计】正式 UI 定位为“现代编辑出版感的私人贴纸书”，而不是可爱手帐工具、照片拼贴软件或传统文件管理器。整体应同时具备书本的收藏感、纸张的温度和现代编辑工具的克制感。

#### 2.4.1 整体视觉系统

- 使用奶油白、暖纸灰、墨黑作为大面积基础色，只选择一种珊瑚红或苔藓绿作为主强调色；另一种颜色仅在封面或贴纸内容中少量出现。
- 书名、章节标题和品牌大标题使用具有编辑出版感的中文宋体或衬线字体；按钮、状态、表单和工具标签使用清晰的无衬线字体。手写字体只用于少量批注、日期或装饰标记，不承担长正文和关键操作。
- 纸张纹理保持低对比，阴影只用于区分真实层级。界面的精致感主要来自字体、留白、比例、照片和贴纸内容，不依赖大量胶带、邮戳或拟物装饰。
- 书与页面采用平面化或轻拟物表达。可以保留书脊、封面厚度、翻页和归架仪式，但不绘制木板房间、卡通家具或沉重的三维场景。
- 浏览状态保持温暖明亮；进入编辑器后可使用稍深的中性工作区承托纸张，使白边贴纸和页面内容更突出，但不得形成专业图像软件的冷硬感。

#### 2.4.2 核心页面方向

- **首页**：以一本正在展开的真实作品作为主视觉，首屏只说明“照片可以变成贴纸，再被收进一本能继续翻开的书”。装饰元素不超过主作品的视觉权重。
- **贴纸工坊**：原图始终是主体。上传后直接进入多主体识别，使用低饱和蒙版、编号和勾选表达选择；底部只保留当前阶段需要的重新识别、点选、框选、描述和生成动作。
- **贴纸预览**：使用透明格纹或中性深浅交替背景检查边缘；多枚贴纸以横向缩略图切换。白边调节、名称和保存动作围绕当前贴纸出现。
- **贴纸仓库**：不做普通文件网格，采用“私人收藏柜”语义；首发围绕个人仓库、官方素材加入、复用、重命名和删除设计。贴纸卡片突出轮廓和收藏感，弱化文件名和技术信息。【已确认/页面事实】不提供自动分类或自定义贴纸包；原型内置素材的演示分类不构成真实自动分类需求。
- **我的书架**：采用干净的封面画廊或书脊列表，不使用仿真木板。每本书主要展示封面、书名、页数、最近编辑时间和一张内容预览；封面本身应成为主要视觉资产。
- **打开一本书**：【已确认/页面事实】用户从书架打开自己的已有手帐时默认先翻阅，仅在主动点击“编辑”后显示编辑工具并允许修改。该模式规则不只适用于首页演示；新建空白手帐仍可直接进入编辑。2026-09-08 确认桌面双页翻阅、移动竖屏单页；纸张有独立正反面。编辑仍聚焦单页，不增加跨书脊编辑。
- **编辑器**：页面画布占据主要空间，进入编辑后才显示精简工具；首发按 PRD 既有范围提供贴纸、文字、纸张和页面操作，不把新增照片元素工具或绘画画笔作为首发要求。对象被选中后才出现 PRD 已确认的复制、层级、删除等上下文工具；白边仍在贴纸生成预览阶段调整，低频设置进入二级面板。
- **页面导航**：桌面端使用可折叠的页面缩略图带，移动端使用底部抽屉。页面支持拖动排序、复制和新增；浏览时可切换单页与双页展开，编辑时聚焦单页。
- **保存与归架**：自动保存只显示轻量的“已保存到本机 / 已同步”状态。离开编辑器使用“放回书架”的产品语言，并以短暂、可跳过的合书或封面回落动效结束创作。

#### 2.4.3 交互约束

- 画布和用户内容始终优先于工具栏；任何时刻只展示完成当前任务所需的主要操作。
- 主流程保持“书架 → 书 → 页面”和“照片 → 主体 → 贴纸 → 收藏 → 页面”两条清晰路径，避免以功能菜单作为产品首页。
- 贴纸制作默认支持一张照片识别并勾选多个主体，一次生成多枚独立、可复用贴纸；该能力应在视觉与文案上作为产品核心特征突出。
- 不以模板市场或 AI 对话作为创作主入口。AI 只在用户需要文案、重新分割或排版辅助时出现，生成结果必须经过预览和确认。
- 不引入社区信息流、公开点赞、关注关系或强制分享。【已确认/页面事实】用户作品导出暂不进入首发，不展示单页、整本或贴纸文件的导出入口；私人保存和再次打开是主路径。此边界不删除既有管理员邀请码/审计导出或账号数据权利处理入口。
- 移动端不把桌面工具栏缩小塞入单屏，应通过底部工具坞、上下文浮层和分步任务降低操作密度。

## 3. 单页面信息架构

【已确认/页面事实】页面按连续旅程纵向组织：

1. 首页：建立价值与气质
2. 贴纸工坊：上传、主体选择、贴纸预览
3. 贴纸仓库：个人贴纸仓库、官方贴纸库
4. 选一本：封面选择与新建手帐
5. 编辑台：取书、开书、纸张、贴纸、文字、多页面
6. 私人书架：归架后的手帐、重命名、删除和重新打开
7. 页脚：品牌、local-first 说明和隐私入口
8. 访问与账号：首次邀请码、手机号注册/登录、会员兑换、同步状态
9. 管理后台：独立 `/admin`，包含邀请码、兑换码、模型接口、账号与用量、审计日志

【建议设计】首页不作为顶部主导航文字项；点击品牌返回首页。四个主导航项保持“贴纸工坊、选一本、编辑台、我的书架”。

## 4. 顶部导航和滚动跳转规则

### 4.1 桌面端

【建议设计】

- 顶部导航固定，使用半透明暖纸背景与轻微模糊。
- 品牌在左，中间为四个锚点，右侧为“开始拾一页”。
- 当前区块通过下划线、颜色或小圆点标识，不使用重型胶囊高亮。
- 点击导航滚动到对应区块标题上方，保留固定头部安全距离。
- 用户手动滚动时，通过区块可见性更新当前导航。
- “开始拾一页”始终前往贴纸工坊，不直接创建数据。

### 4.2 移动端

【建议设计】

- 顶部仅保留品牌、主 CTA 和菜单按钮；不把四个导航项硬塞进窄屏。
- 菜单采用下拉纸签或底部抽屉，包含四个区块锚点。
- 选择后关闭菜单并滚动到目标区块。

### 4.3 动效与可访问性

【已确认/页面事实】默认平滑滚动；用户开启减少动态效果时立即跳转。锚点必须可通过键盘聚焦，焦点落在目标区块标题或首个关键控件。

## 5. 每个页面区域的目标和内容结构

### 5.1 首页

目标：在首屏解释产品，不要求用户先理解“抠图 API”或复杂编辑能力。

内容层级：

1. 品牌短句
2. 核心标题：把照片中的日子收进可以再翻开的书
3. 一段产品解释
4. 主 CTA“制作第一枚贴纸”
5. 次 CTA“先看看书架”
6. 书本、贴纸和邮戳组成的视觉演示
7. 向下浏览提示

【已确认/页面事实】现有原型已经采用上述结构和“一本书，不是一个文件”的表达，可沿用方向，但具体文案仍可由品牌阶段统一校正。

### 5.2 贴纸工坊

目标：让用户完成“上传 → 选主体 → 调白边 → 保存”的单一任务。

内容层级：上传区、处理进度、主体选择区、贴纸预览区、保存操作。

### 5.3 贴纸仓库

目标：让用户理解贴纸可复用，并区分“个人收藏”和“官方可选素材”。

内容层级：个人仓库标签、贴纸数量、个人贴纸列表、官方贴纸库入口、下一步选书 CTA。

### 5.4 选一本

目标：为新手帐选择封面并建立“取书”仪式。

内容层级：区块标题、封面卡片、选中信息、书名初值、取书 CTA。

### 5.5 编辑台

目标：以尽量低的界面负担完成逐页创作。

内容层级：书本转场、编辑顶部栏、纸张选择、素材/文字工具、页面画布、元素工具、页面管理、保存状态、归架动作。

### 5.6 私人书架

目标：让用户看见完成成果，重新打开、重命名或删除手帐。

内容层级：空状态或书架、书本列表、书本详情动作、本地存储说明。

## 6. 完整交互流程

### 6.1 新建旅程

未绑定邀请码验证（已有账号可直接手机号登录）→ 游客进入首页 → 上传照片 → 自动候选 → 多选或用点选/框选/一句话补救 → 分别生成贴纸 → 调白边 → 保存到个人仓库 → 选择或加入官方贴纸 → 选封面 → 取书 → 开封面 → 选纸张 → 添加贴纸/文字 → 可选 AI 文案 → 可选会员 AI 排版 → 多页面创作 → 本地自动保存 → 注册/登录并选择导入本机作品 → 异步云同步 → 合上并归架 → 书架出现手帐。

### 6.2 回访旅程

进入书架 → 确定当前身份可访问的数据范围 → 读取对应游客/账号本地数据并在登录后检查该账号云端版本 → 选择一本已有手帐 → 加载 → 轻量开书 → 默认翻阅当前页 → 继续翻阅，或主动点击“编辑” → 修改后本地自动保存与可选云同步 → 再次归架。

### 6.3 非阻断原则

【建议设计】个人仓库为空时，用户仍可以创建纯文字手帐；主流程提示其先制作贴纸，但不弹出硬性阻断。

### 6.4 状态恢复

【建议设计】刷新后只恢复当前身份可访问的、最近成功保存在本机的书、页面和元素；登录用户联网后再检查其云端 revision。若两端分叉，展示“保留本机版本 / 保留云端版本 / 两份都保留”，不得静默覆盖。上次在编辑中直接关闭时可显示“继续上次编辑”，但需用户主动点击才进入编辑；普通重开仍先翻阅，不自动播放归架动画。退出后的账号缓存不得作为游客恢复内容。

## 7. 图片上传与贴纸制作工坊

### 7.1 默认空状态

【建议设计】上传卡以纸片或相片框形式呈现：

- 图标：加号或相片符号
- 主文案：“上传一张想留下的照片”
- 辅助文案：支持格式与大小限制
- 隐私提示：“照片将发送给第三方服务进行主体提取”
- “了解图片处理方式”文本入口

### 7.2 选择文件

【合理推断】桌面端点击或拖放；移动端点击打开系统文件选择。MVP 未要求直接调用相机拍摄，因此不纳入本期交互。

【建议设计】文件选择后立即显示本地缩略图和文件名，但在通过格式、大小和解码校验前不发出 API 请求。

### 7.3 处理阶段

界面按真实阶段显示：

1. 正在读取照片
2. 正在临时上传
3. 正在排队
4. 正在识别主体
5. 已找到候选主体
6. 正在生成所选贴纸

【建议设计】使用细进度轨和阶段文案，不展示虚假百分比。排队后保留 jobId，允许取消任务或离开后回来查看；超过预期时间后显示“比平时久一些”，并提供取消。

### 7.4 多主体选择

【已确认/页面事实】一张照片可选择多个主体，每个选中主体分别生成一枚贴纸。

【建议设计】

- 原图置于主体选择画布中央。
- 候选主体使用不同但低饱和的半透明蒙版，未选中时仅显示细轮廓。
- 悬停或触摸时高亮该主体；点击切换勾选。
- 选中主体显示白色外轮廓、角标编号和勾选标记。
- 顶部显示“已选择 N 个主体”。
- 主按钮随数量更新为“生成 N 枚贴纸”。
- 候选重叠时，桌面端可连续点击切换重叠候选；移动端提供候选缩略图列表辅助选择。
- 至少选中一个主体后主按钮才可用。
- 系统只提供选择结果，不使用“人物”“建筑”等类别名称，除非供应商确实返回可靠类别；不得伪造语义识别。
- 候选区固定提供“没找到？重新选择主体”入口，展开后显示“点一下”“框一下”“圈出轮廓”“用一句话描述”四种模式。
- 点一下：单击/触摸原图给出正向点；允许撤销最近一点。是否开放负向点由技术验证决定，首版界面不得强迫用户理解算法术语。
- 框一下：拖出矩形框；移动端提供清晰的起止反馈和“重新框选”。框选完成后先显示蒙版预览，再由用户确认。
- 一句话：短输入框示例“右边戴帽子的人”“桌上的红色杯子”；提交后先显示定位框/点，再生成蒙版。结果不对时可改写、点选或框选。
- 一句话次数在当前图片附近展示“本图剩余 N 次”；失败不减少显示值。达到上限后仅禁用自然语言入口，点选和框选仍可用。
- 【已确认/页面事实】免费账号每图 3 次，会员每图总计 10 次；注册时当前图保留游客已用次数，新图使用账号对应每图额度。该能力不按月重置，不显示“下月恢复”；游客状态另外说明“本邀请码累计体验剩余 N 次”，不将累计游客额度误标为每图额度。

### 7.5 无候选或候选错误

【建议设计】

- 无候选：说明“没有找到清晰主体”，提供点选、框选、圈选轮廓、一句话描述、重新识别和换一张照片。
- 候选太多：按面积、稳定性和重复度过滤，分批显示全部有效主要物体候选，不用固定数量静默隐藏主要物体。
- 主体漏提、不完整或提错：P0 支持点选、框选或手绘大致轮廓重新分割，由 AI 自动贴合真实边缘。圈选可撤销、重画并预览新蒙版；不要求精细描边。逐像素高级精修与手帐绘画仍不在首发范围。
- 部分主体生成失败：保留成功贴纸，失败卡片单独重试。

## 8. 白边调节和贴纸预览

### 8.1 单枚预览

【建议设计】透明贴纸置于带浅格纹的预览区，便于看清透明边缘。预览不使用与白边相同的纯白底。

### 8.2 调节控件

【已确认/页面事实】提供窄、自然、宽三种快捷值。

【建议设计】同时提供连续滑杆；若 P0 排期不足，可先实现三档，滑杆进入 P1。控件变化应在 100ms 量级内更新预览，不重新调用第三方 API。

### 8.3 多枚预览

【建议设计】

- 使用横向缩略图或步骤标签切换贴纸。
- 每枚贴纸独立保存白边值。
- 提供“应用到全部”辅助动作，但不得默认覆盖用户已逐枚修改的值。
- 主按钮显示“保存 N 枚到个人仓库”。

### 8.4 保存成功

【建议设计】保存成功后贴纸缩略图以短距离落入个人仓库区域；同时给出文字反馈。减少动态效果时只更新列表和状态，不飞行动画。

## 9. 贴纸仓库

### 9.1 信息分区

【已确认/页面事实】仓库分为：

- 个人贴纸仓库：初始为空，存放用户上传生成或主动加入的贴纸。
- 官方贴纸库：产品内置 Emoji 等只读素材，不是社区。

### 9.2 个人仓库空状态

【建议设计】文案：“这里还没有你的贴纸。”提供“上传照片”和“去官方贴纸库看看”两个动作。

### 9.3 贴纸卡片

卡片内容：贴纸缩略图、名称、更多操作。桌面悬停和键盘聚焦显示操作；移动端长按或点击更多按钮。

操作：

- 添加到当前页（编辑器内）
- 重命名
- 从个人仓库删除

【建议设计】删除已在页面使用的贴纸时，确认文案为：“这枚贴纸仍在 N 个页面中使用。移出仓库不会删除页面中的贴纸。”

### 9.4 官方贴纸库

【建议设计】

- 官方贴纸展示“加入”按钮；已加入显示“已收藏”。
- 加入操作幂等，不重复制造相同个人副本。
- 个人副本可重命名和删除，官方卡片保持不变。
- 不出现上传、点赞、作者主页、评论或分享等社区暗示。

### 9.5 仓库滚动

【建议设计】桌面端可使用横向收藏抽屉或网格；移动端使用两列网格。贴纸多时分页或虚拟滚动由性能测试决定，不在界面中预设无限社交流。

## 10. 封面选择

### 10.1 封面卡片

【已确认/页面事实】原型展示苔绿、杏色、靛蓝三种封面，可作为初始视觉方向，但数量尚未最终确认。

【建议设计】每张卡片包含封面视觉、风格名称和默认书名。选中状态使用边框、轻微抬升和文本“已选择”，不能仅依靠颜色。

### 10.2 新建时机

【建议设计】浏览和切换封面不创建数据。点击“从书架取下这本书”时创建新手帐并启动取书动画，防止产生无意义草稿。

### 10.3 书名

【已确认/页面事实】手帐支持重命名。

【建议设计】新建时使用封面默认书名；进入编辑后可修改。空书名失焦时回退默认书名。

## 11. 取书和开书动画

### 11.1 动画状态机

【建议设计】状态顺序：

静止封面卡 → 从架位滑出 → 轻微旋转并居中放大 → 封面打开 → 书页稳定（已有手帐：翻阅；新建空白手帐：编辑）。

【已确认/页面事实】已有手帐无论正常播放、跳过或减少动态效果，都先进入翻阅；不因动画路径不同自动显示编辑工具。

### 11.2 触发条件

- 新建：点击“从书架取下这本书”。
- 重开：点击私人书架中的书。
- 重复点击期间无效，防止创建两本书或重复启动动画。

### 11.3 操作锁定

【建议设计】动画期间锁定编辑和导航主操作，但“跳过”始终可用。页面滚动可临时锁定，避免用户滚走导致状态断裂。

### 11.4 跳过

【建议设计】动画开始后在右上角显示“跳过动画”。键盘 Escape 同样跳过。跳过只改变视觉进度，不取消书本创建或加载。

### 11.5 结束门槛

【建议设计】只有书本记录已创建或旧书数据已成功加载，才能进入稳定书页；已有手帐先进入翻阅，再由用户主动进入编辑。加载失败时停止在安全状态并显示重试，不得进入空白假书。

## 12. 纸张选择

### 12.1 页面级设置

【已确认/页面事实】每一页独立保存纸张。切换当前页纸张不影响其他页面。

### 12.2 初始纸张

【已确认/页面事实】现有原型包含奶油空白、细点、方格和横线。建议沿用为初始集合，最终数量尚未确认。

### 12.3 选择控件

【建议设计】桌面端在编辑顶部显示纸张缩略块；移动端放入“纸张”底部面板。每个缩略块需要文字标签或可访问名称，不能只显示纹理。

### 12.4 切换反馈

【建议设计】点击后立即更新画布，选中态使用墨绿描边加勾选。切换产生自动保存，不重新排列页面元素。

## 13. 手帐编辑画布

### 13.1 编辑模式

【已确认/页面事实】已有手帐默认处于“翻阅”：隐藏编辑工具、选框及变换控制，不允许拖动、删除或修改页面内容；保留翻页、进入编辑和归架入口。点击“编辑”后才允许操作；新建空白手帐可直接编辑。模式切换不重建页面、不改变纸张或元素位置。

【建议设计】从编辑切回翻阅时先完成本地保存；失败则保留编辑状态和内容并提供重试，不静默丢弃。重开不继承上次处于编辑的模式标记。

【建议设计】MVP 每次编辑一个竖向单页，不做真实双页跨页编辑。外层保留书本、桌面和页码语义；核心画布保持平面状态。

### 13.2 桌面布局

- 顶部栏：书名、翻阅/编辑切换、保存状态、归架
- 次级栏：纸张选择
- 左侧工具抽屉：贴纸、文字
- 中央：单页画布
- 画布旁或元素附近：上下文工具条
- 底部：页面缩略图、新增和翻页

【已确认/页面事实】原型已经采用工具抽屉、中央画布、选择工具条和页面控制的基本结构，可作为布局依据。

### 13.3 画布尺寸与坐标

【建议设计】使用固定逻辑纵横比和响应式显示尺寸。元素位置与尺寸保存为画布归一化值，缩放窗口不改变构图。

### 13.4 安全区与越界

【建议设计】元素可以部分超出页面形成剪贴效果，但其选择中心或至少 20% 可交互区域必须留在画布内。超出部分裁切显示。

### 13.5 选择规则

- 点击元素：单选
- 点击空白：取消选择
- 新增元素：自动选中并置于可见中心附近
- 页面切换：清除旧页选择
- MVP 不支持多元素框选或组合

## 14. 贴纸选择、拖动、缩放、旋转、复制、删除和层级规则

### 14.1 添加

【已确认/页面事实】支持从编辑器内贴纸匣直接拖到纸页落点，也可点击添加到当前页中央附近；手机支持点击添加或长按后拖入，短滑动仍用于滚动素材列表；连续添加产生轻微错位，避免完全重叠。官方贴纸需先加入个人仓库再用于页面，以保持资产模型一致。

### 14.2 拖动

- 指针按下后进入拖动，拖动超过轻微阈值才视为移动。
- 拖动时不连续写 IndexedDB，只更新内存和 DOM；松开后触发保存。
- 移动端禁用画布内浏览器默认平移，页面外围仍可滚动。

### 14.3 缩放

【建议设计】桌面端提供角部控制柄与工具条加减；移动端支持双指缩放，并保留底部元素工具条替代操作。保持贴纸原始宽高比，不允许非等比拉伸。

### 14.4 旋转

【建议设计】桌面端提供旋转柄或工具条；移动端支持双指旋转，并保留左转/右转按钮。按钮按固定步长旋转，控制柄可连续旋转。具体步长和上下限通过可用性测试确定。

### 14.5 复制

【已确认/页面事实】复制创建独立页面实例，使用相同贴纸资产。复制件相对原件轻微偏移、自动选中并置于原件上方。

### 14.6 删除

【建议设计】页面中的“删除”仅删除实例，不影响个人仓库。删除可立即执行并显示短暂撤销提示；若不实现撤销，则删除按钮需要与其他操作拉开距离并提供键盘 Delete/Backspace。

【未知/尚未确认】通用撤销/重做未被纳入已确认 MVP；如不实现，设计不得展示不可用的撤销按钮。

### 14.7 层级

操作完整集合：

- 上移一层
- 下移一层
- 置于顶层
- 置于底层

【建议设计】层级变化应即时可见，并在保存后保持。新添加或复制元素默认置顶。

### 14.8 选中视觉

【建议设计】使用杏色虚线边框、控制柄和轻量上下文工具条。选中框不进入最终页面内容，也不改变元素实际边界。

## 15. 文字创建、编辑和排版规则

### 15.1 创建

【建议设计】“文字”面板包含输入框、字体风格和“添加到这一页”。空内容时按钮禁用或点击后把焦点放回输入框并提示。

### 15.2 二次编辑

【已确认/页面事实】已有文字必须可编辑。桌面双击、移动端双击或上下文“编辑文字”打开浮层/底部面板。提交后原位更新，不创建新元素。

### 15.3 字体与排版

【建议设计】MVP 使用三种预设：手写感、宋体、简洁无衬线。预设决定字体栈、基础字号和行高；用户可通过元素缩放改变整体大小。

规则：

- 保留换行和连续段落。
- 默认左对齐；不提供复杂段落样式。
- 文本框宽度使用有限范围，超长文本自动换行。
- 不支持混合字体、局部加粗、超链接、列表或内嵌图片。
- 文字颜色以深墨绿/近黑为主；额外颜色是否开放为【未知/尚未确认】。

### 15.4 文字元素变换

【建议设计】文字与贴纸共享选择、移动、缩放、旋转、层级、复制和删除逻辑。缩放改变整体视觉比例，不在拖动时重新排版导致跳动。

## 15A. AI 文案

### 15A.1 入口与输入

【已确认/页面事实】文字面板同时提供“自己写”和“AI 帮我”两个清楚但不抢夺注意力的入口。用户已有草稿时按钮为“帮我润色”，输入为空时为“帮我写一段”。任何页面打开、拖动或自动保存都不得自动调用模型。

【建议设计】AI 面板显示日期、地点、心情和语气。地点默认为手动输入的粗粒度名称，不默认申请 GPS。输入范围说明写明：“AI 只会参考当前页贴纸缩略图和你填写的信息。”

### 15A.2 生成预览

- 生成中保留用户草稿，不清空输入框。
- 结果进入独立预览卡，动作包括“放到页面”“再来一版”“继续修改”“取消”。
- 默认长度 40–100 个中文字符；语气为自然记录、温柔手帐、简短克制。
- “放到页面”后生成普通文字元素，可继续编辑、移动、缩放和删除。
- 免费用户显示每个北京时间自然月 10 次，会员显示当月总计 100 次；当月升级前和同邀请码游客阶段已使用次数包含在会员额度内。
- 文案通过校验并进入预览卡时扣一次；即使用户随后取消也计次。“再来一版”发起新请求并再次计次；系统失败、超时、空结果或结构无效不扣。
- 预览卡和放入页面后的文字属性显示克制的“AI 辅助”来源，不把用户后续编辑伪装成从未经过 AI。

## 15B. 会员 AI 自动排版

### 15B.1 入口与风格

【已确认/页面事实】入口包含“智能排版新页面”和“重新排版当前页”。非会员点击时展示会员权益与兑换码入口，不跳转在线支付。

风格选择：自动选择、留白记录、杂志拼贴、随性手帐。每种风格显示小型结构示意，不使用可能与真实结果不一致的精美成品冒充预览。

“智能排版新页面”先进入素材选择步骤：用户从个人仓库勾选贴纸，并选择是否带入已有文案。界面明确说明 AI 不会浏览整个仓库，也不会自行生成、修改或删除文字。候选生成期间不创建空白正式页面，应用某个候选时才在当前页之后创建新页。

### 15B.2 生成、比较和应用

- 点击生成前显示本月剩余次数；一次最多产生 3 个候选布局。
- 生成期间用户仍可继续编辑，但若页面 revision 已变化，结果标记为“基于旧版本”，不能直接应用。
- 候选使用临时幽灵层或复制画布预览，不写入正式页面。
- 用户可以切换候选、应用、放弃；切换同批候选不重复扣次。
- 应用前保存页面快照；应用后提供“一键撤销此次排版”。
- locked 元素显示锁定标识并保持原位；AI 只能安排未锁定元素。
- 排版结果仍是独立贴纸、文字和纸张参数，不生成不可编辑的整页图片。
- 当前页重排只调整未锁定元素的位置、大小、角度和层级，可提供纸张建议；不得新增、删除或改写现有文字和贴纸。

### 15B.3 额度反馈

【已确认/页面事实】会员每个北京时间自然月 30 次，不结转。一次返回至少一个可渲染合格候选即计一次，用户放弃仍计次；重新发起才再次计次；系统失败、超时或全部候选结构校验不通过不扣。额度耗尽时保留手工编辑，不锁住页面。

## 16. 多页面创建、切换、排序和删除

### 16.1 页面条

【建议设计】桌面底部显示页面缩略图条；移动端使用可横向滚动的缩略图。每张显示页码、纸张纹理和简化内容预览。

### 16.2 新增

【已确认/页面事实】点击“新增一页”在当前页之后插入空白页，并自动切换。新页默认使用奶油空白纸或最近使用纸张，二者需选定；建议默认奶油空白，避免无意复制。

### 16.3 切换

【建议设计】切换前强制提交当前文字编辑并刷新内存状态；然后切换页面并触发保存。页面转场约 250–350ms，不阻断连续点击。

### 16.4 排序

【已确认/页面事实】桌面拖动缩略图排序；移动端拖动或使用“前移/后移”。排序只修改 pageOrder，不重建页面 ID。

### 16.5 删除

【已确认/页面事实】

- 一本书至少保留一页。
- 删除有内容页面必须二次确认。
- 确认信息包含页码和内容概况。
- 删除当前页后打开前一页；若不存在则打开新的第一项。

### 16.6 页数限制

【未知/尚未确认】原型每本最多 20 页，仅为实现参考。正式上限需根据 IndexedDB、缩略图和移动端性能测试决定。

## 17. 自动保存反馈

### 17.1 状态集合

【建议设计】编辑顶部栏显示：

- 正在保存…：中性点或轻微脉冲
- 已保存到本机：本地事务成功，游客或等待同步
- 正在同步：登录用户的本地变更正在提交云端
- 已同步：云端已确认当前 revision
- 同步失败：本地内容仍在，显示“稍后重试”
- 保存失败：错误色、原因摘要、“重试”
- 本地空间不足：错误色、“管理贴纸/手帐”入口

### 17.2 触发反馈

【建议设计】输入、拖动和变换期间显示“正在保存…”，但不频繁闪烁。停止操作后约 800ms 写入本机；成功才显示“已保存到本机”。登录且联网时继续显示同步状态。页面切换和归架前立即保存本机数据，云同步可以在后台继续。

### 17.3 失败语义

【建议设计】保存失败时不自动消失、不显示成功 Toast、不继续成功归架动画。内存中的当前内容尽量保留，允许用户重试或先停留页面。

### 17.4 离线

【合理推断】已加载的本地手帐仍应可编辑和保存；新照片抠图、AI 文案、AI 排版和云同步不可用。显示“离线中，内容会保存到本机；联网后再同步。AI 功能需要联网”。

## 18. 退出编辑和放回书架

### 18.1 主动作

【已确认/页面事实】编辑顶部使用“合上并归架”，而不是通用“保存并退出”，保持产品语义。

### 18.2 执行顺序

【建议设计】

1. 结束当前文字输入和元素拖动。
2. 立即保存当前书和页面。
3. 保存成功后合上页面和封面。
4. 书本缩小、轻旋并滑回书架。
5. 滚动或切换到私人书架，目标书短暂高亮。

### 18.3 保存失败

【建议设计】停留在编辑器，显示“暂时无法安全归架”。提供重试和继续编辑；不得播放完整归架成功动画。

### 18.4 浏览器直接关闭

【合理推断】无法保证播放动画。系统在页面隐藏或关闭前尽力保存；下次进入时恢复最后成功版本，不声称已经完成归架动画。

## 19. 书架浏览和重新打开

### 19.1 空书架

【已确认/页面事实】显示空架位、简短说明和“去选一本书”。不要放虚假书本占位。

### 19.2 书本排列

【建议设计】按最近编辑时间排序。桌面以书脊竖排；移动端横向滚动书脊或使用两列封面卡，确保书名可读。

### 19.3 书本信息

聚焦或点击后显示：书名、封面、页数、最近编辑时间、打开、重命名、删除。

### 19.4 重新打开

【已确认/页面事实】点击书本加载当前身份有权访问的最近本地状态，播放轻量开书动画并以翻阅模式进入上次页；点击“编辑”后才允许修改。若资源缺失，显示具体缺失状态，不静默删除元素；其他账号的缓存不得进入加载或预览。

### 19.5 重命名

【已确认/页面事实】支持从书架菜单和编辑顶部修改书名。空值回退默认名；修改后立即自动保存。

### 19.6 删除

【已确认/页面事实】删除会移除本地手帐；登录用户还会同步云端软删除。确认框显示书名、页数和恢复边界。默认焦点停留在取消按钮；删除按钮使用克制错误色，不用品牌主色。

## 19A. 邀请、登录、同步与会员兑换

### 19A.1 首次邀请码

【已确认/页面事实】内测阶段无账号访问者展示独立的暖纸卡片：品牌、用途说明、邀请码输入和“进入拾页”。同一未绑定邀请码可以在不同手机、电脑和浏览器验证进入；每个浏览器的游客作品彼此独立。卡片同时提供“已有账号？手机号登录”，已有账号不需要邀请码。

错误状态区分无效、过期、已停用和已绑定，但不暴露绑定手机号、邀请码批次或其他账号信息。连续错误触发冷却，不使用无限重试。邀请码绑定手机号后，当前及其他浏览器中的邀请访问资格在下一次服务端校验时失效。

### 19A.2 游客与注册

【已确认/页面事实】邀请码通过后进入游客模式，可本地制作。首次需要完整 AI 权益、会员兑换或跨设备同步时打开登录面板，不在首屏强迫注册。

注册流程：手机号 → 短信验证码 → 同意用户与隐私条款 → 完成账号创建。之前验证的邀请码由系统自动绑定，不要求再次输入；绑定成功后邀请码立即失效，不能再进入游客模式或绑定第二个手机号。首版注册和登录只使用短信验证码，不提供设置密码、忘记密码或密码重置入口。

游客升级注册或登录时先展示“是否将本机作品导入账号”，列出本机手帐和贴纸供用户勾选，默认不把未选内容上传。导入对象生成新的云端 ID；云端确认成功前保留本地副本，成功后才标记归属账号并保持当前编辑位置。其他浏览器的游客作品需要在对应浏览器登录后分别导入。

【已确认/页面事实】邀请码绑定前，各浏览器共享该邀请码累计体验额度：自动主体识别 5 张照片、点选/框选 10 次、一句话选主体 3 次、AI 文案 3 次；游客不能使用 AI 排版。额度耗尽只禁用对应联网能力并引导注册，不影响已有内容的本地编辑。注册后，一句话选主体同图继承已用次数，新图使用账号每图额度，不按月重置；游客 AI 文案用量仍计入账号注册当月的文案额度。

### 19A.3 会员兑换

- 入口位于账号菜单、会员权益页和非会员 AI 排版提示中。
- 输入有效兑换码后先展示将获得的会员类型与有效期，再确认兑换。
- 当前会员未到期时展示“将在现有到期日后顺延”；会员已到期或非会员则从兑换成功时间起算。允许连续兑换多个有效码累计时长。
- 成功后更新到期时间、排版额度和文案额度；重复、过期、停用或已用尽的码给明确状态。当月升级会员后，文案上限提升为当月总计 100 次，升级前已用次数包含在内。
- 首版不展示付款按钮、价格或自动续费承诺。

### 19A.4 账号与隐私入口

【已确认/页面事实】账号菜单提供“退出登录”。退出保留当前账号已保存的本地作品，但立即隐藏并阻止访问其书、贴纸、缩略图和最近打开记录；再次登录同一账号后才恢复可见。游客和其他账号不可访问这些数据，也不能把原账号的未同步任务提交到新账号。退出不是删除或注销，也不承诺浏览器清理后的恢复。

【建议设计】退出前说明：“作品会保留在这台设备，重新登录同一账号后可继续查看。”若仍有未保存内容，先保存并反馈结果；失败时提供重试或明确的放弃未保存改动确认，不默默丢弃。会话撤销异常不得显示完整退出成功，细节见技术方案第 5.1 节。账号与数据保留查阅、更正、复制及申请注销/删除云端数据入口；这些入口不等于新增用户作品导出功能。注销使用短信再次验证，冷静期与法定日志保留边界仍待上线前确认。

## 19B. 管理后台

### 19B.1 访问与布局

【已确认/页面事实】管理员使用独立 `/admin` 路由。未登录返回管理员登录；普通用户返回 403 说明页，不渲染后台数据。桌面端采用左侧导航、顶部状态区和主内容表格；移动端只保证紧急查询和停用操作可用，批量管理优先桌面。

### 19B.2 概览

首屏展示：注册用户数、活跃会员、今日/本月 AI 调用、成功率、估算成本、异常模型、即将耗尽预算。指标卡可进入明细，不展示用户作品内容。

### 19B.3 邀请码和兑换码

- 支持单个/批量创建、复制、导出、有效期、备注和启停；每个邀请码只允许绑定一个手机号账号，列表区分未绑定、已绑定、已停用和已过期。
- 完整码仅在创建结果中展示；列表之后只显示前后掩码。
- 可查看跨浏览器验证次数、共享游客额度和最终绑定账号，但不得显示明文邀请码或完整手机号。
- 兑换码额外展示会员方案、有效天数、已兑换人数和兑换记录。
- 停用不等于删除；已兑换会员不因停用兑换码而自动撤回。

### 19B.4 AI 接口管理

- 按“自动分割、交互分割、视觉定位、文案、排版”查看当前主模型与备用模型。
- 支持供应商、模型名、启停、优先级、超时、有限重试、单价版本和健康测试。
- API Key 输入后不可再次明文读取；只显示掩码和最近更新时间。
- 首版使用受支持供应商模板和服务端 endpoint allowlist，不允许前端随意提交任意 URL。

### 19B.5 账号与用量

- 搜索账号、查看邀请来源、账号状态、会员到期日、各项 AI 额度、云端对象数量与存储字节。
- 按日期、能力和模型查看调用次数、成功/失败、耗时、token/调用量和成本。
- 支持停用/启用、调整会员与额度；操作前确认，操作后写审计日志。
- 停用不删除作品；管理员默认不能打开用户原图、贴纸像素或手帐正文。

## 20. 动画时长和缓动建议

### 20.1 核心动画表

| 动画 | 建议时长 | 建议缓动 | 操作状态 |
|---|---:|---|---|
| 区块进入轻浮现 | 450–700ms | ease-out | 可滚动，不阻断 |
| 书本滑出架位 | 400ms | cubic-bezier(0.22, 0.7, 0.2, 1) | 锁定，允许跳过 |
| 书本居中、旋转、放大 | 350ms | cubic-bezier(0.2, 0.75, 0.2, 1) | 锁定，允许跳过 |
| 封面打开 | 650ms | cubic-bezier(0.15, 0.7, 0.15, 1) | 锁定，允许跳过 |
| 第一页轻翻与编辑器进入 | 400ms | ease-out | 完成前锁定 |
| 普通页面切换 | 250–350ms | ease-in-out | 可继续排队一次操作 |
| 合书 | 450ms | ease-in-out | 保存成功后锁定 |
| 书本归架 | 500ms | cubic-bezier(0.3, 0, 0.25, 1) | 锁定，允许跳过 |
| Toast 进入/退出 | 180/220ms | ease-out/ease-in | 不阻断 |

【建议设计】取书到编辑稳定总时长约 1.6–2.0 秒，可通过阶段重叠控制；不要简单把各段串联成过长等待。

### 20.2 减少动态效果

【已确认/页面事实】检测 prefers-reduced-motion。所有大位移、3D 翻转、漂浮和持续循环动画改为约 150ms 淡入/淡出；平滑滚动改为立即跳转。

### 20.3 低性能降级

【建议设计】根据设备能力或运行帧率减少多层阴影、背景颗粒混合、透视、模糊和并发动画。核心状态变化必须保持，不以动画完成作为数据完成依据。

## 21. 桌面端与移动端响应式规则

### 21.1 断点原则

【已确认/页面事实】现有原型使用约 1020px 和 760px 两级断点，可作为参考，不是最终设备标准。

【建议设计】最终断点由内容拥挤程度决定：

- 宽桌面：双栏首页、双栏工坊、三列封面、左侧工具抽屉
- 平板/窄桌面：减少间距、工具抽屉变窄或可折叠
- 移动：单列旅程、封面纵排、工具变为底部抽屉、页面缩略图横向滚动

【已确认/页面事实】桌面和移动使用同一套业务数据、权限、API 与功能范围，不维护两个独立产品。断点只改变布局和操作方式，不减少桌面或移动的核心能力。

### 21.2 桌面端编辑器

【建议设计】宽屏采用左侧素材栏、中间画布、右侧属性/AI 面板、底部页面条。鼠标拖动负责移动；控制柄负责缩放和旋转；滚轮默认滚动页面，只有在明确画布缩放模式或按住组合键时缩放，避免误操作。

键盘至少支持 Delete/Backspace 删除非文字编辑状态下的选中元素、Escape 退出面板/选择、方向键微移、Cmd/Ctrl+Z 撤销 AI 排版应用。复制快捷键是否进入首发由可用性测试决定，界面动作必须始终存在。

### 21.3 移动端编辑器

【已确认/页面事实】2026-09-08 已确认加入双指缩放与旋转；同时保持单指、控制柄和工具条可完成全部核心操作，不强迫用户使用双指。

【建议设计】

- 顶部：返回/书名、保存状态、归架
- 中部：尽可能大的页面画布
- 底部：贴纸、文字、纸张、页面四个工具入口
- 选中元素后底部显示移动之外的缩放、旋转、复制、层级和删除
- 工具面板展开时不遮住被选元素；必要时画布上移
- 触控目标最小 44×44 CSS px

### 21.4 字号与留白

【建议设计】移动端减少装饰而不是压缩正文。标题允许自然换行；正文不低于 14px；主操作不与系统底部手势区重叠。

## 22. 字体、颜色、间距、圆角、纹理和阴影规范

### 22.1 字体

【已确认/页面事实】现有原型使用宋体、楷体/手写感和无衬线字体栈。

【建议设计】

- 品牌与大标题：Songti SC / STSong / Noto Serif CJK SC / serif
- 手写标记与部分手帐文字：STKaiti / KaiTi / Kaiti SC / cursive
- 功能 UI：Inter / PingFang SC / Microsoft YaHei / system-ui
- 字体加载失败时必须使用同类回退，不依赖单一外部字体服务

### 22.2 颜色

【已确认/页面事实】现有原型颜色不作为正式 UI 的直接继承方案。正式视觉先采用以下基础令牌，具体色值仍需通过高保真稿和对比度检查收敛：

| 令牌 | 参考色 | 用途 |
|---|---|---|
| Ink | #1F211F | 主文字、轮廓、深色工具 |
| Ink Soft | #555852 | 次文字、说明文字 |
| Cream | #F3EDE2 | 应用主背景 |
| Paper | #FBF8F1 | 页面与纸张 |
| Paper Deep | #E6DED0 | 分区、描边和工具背景 |
| Moss | #486052 | 可选主强调色、成功与品牌动作 |
| Coral | #D9674F | 可选主强调色、选中和手写标记 |
| Workspace | #292A28 | 可选编辑工作区背景 |

【建议设计】珊瑚红和苔藓绿只能选择一种作为全局主强调色，另一种降为封面或内容辅助色。错误状态使用独立低饱和红色，成功状态使用可读绿色；主强调色不能同时承担错误语义。颜色对比需通过 WCAG AA 检查。

### 22.3 间距

【建议设计】采用 4px 基础网格。常用间距：4、8、12、16、24、32、48、64。大区块垂直间距桌面 96–120px，移动 64–88px。

### 22.4 圆角

【建议设计】纸片、按钮和面板以 2–8px 轻圆角为主；胶囊仅用于小标签和顶部 CTA；书本封面可使用不对称 4–16px 模拟书脊。

### 22.5 纹理

【建议设计】纹理必须轻量、低对比、不可干扰文字。优先 CSS 渐变和小型可平铺资源；不使用高分辨率大背景图。减少透明混合以降低移动端成本。

### 22.6 阴影

【建议设计】

- 普通卡片：短距离、低透明阴影
- 书本和编辑画布：较长柔和阴影
- 选中元素：使用明确细轮廓和操作柄，辅以低饱和柔光；不只依靠阴影表达选中
- 同一视图最多两级主要阴影，避免漂浮感过强

## 23. 组件清单

### 23.1 全局组件

- BrandLockup
- TopNavigation
- MobileNavigationDrawer
- PrimaryButton / SecondaryButton / TextButton / DestructiveButton
- SectionHeading
- StatusToast
- ConfirmDialog
- InlineNotice
- LoadingSkeleton
- EmptyState
- ErrorState
- InviteGate
- AuthSheet / AuthDialog
- GuestWorkImportDialog
- AccountAndDataPanel / LogoutAction / AccountDeletionRequest
- MembershipRedeemDialog
- SyncStatus

### 23.2 贴纸组件

- PhotoUploader
- UploadValidationMessage
- ProcessingStage
- DirectUploadProgress / AsyncJobStatus
- SubjectSelectionCanvas
- SubjectMaskOverlay
- SubjectCandidateThumbnail
- StickerPreview
- BorderPresetGroup
- BorderSlider
- MultiStickerPreviewStrip
- PersonalStickerCard
- OfficialStickerCard
- StickerRenameDialog
- StickerDeleteDialog
- SubjectRefineModePicker
- PointPromptOverlay
- BoxPromptOverlay
- NaturalLanguageSubjectInput

### 23.3 手帐与编辑组件

- CoverCard
- BookOpeningStage
- SkipAnimationButton
- EditorTopBar
- SaveStatus
- PaperPicker
- ToolDrawer / MobileToolSheet
- JournalCanvas
- CanvasElement
- SelectionBounds
- ElementToolbar
- TextEditorPanel
- PageThumbnailStrip
- PageThumbnail
- AddPageButton
- PageDeleteDialog
- ShelfBook
- ShelfBookMenu
- EmptyShelf
- AICopyPanel
- AICopyPreview
- AILayoutStylePicker
- AILayoutCandidatePreview
- LayoutUndoToast

### 23.4 管理后台组件

- AdminShell / AdminNavigation
- AdminOverviewCards
- InviteCodeTable / InviteCodeCreateDialog
- MembershipCodeTable / MembershipCodeCreateDialog
- ProviderRouteTable / ProviderSecretDialog / ProviderHealthCheck
- UserUsageTable / UserDetailDrawer
- QuotaAdjustmentDialog / AccountStatusDialog
- AuditLogTable

【建议设计】组件名为功能性建议命名，不代表现有代码或未来框架中的正式组件名。

## 24. 加载、空白、错误、离线和异常状态

### 24.1 图片与 API

| 状态 | 表现 | 主要动作 |
|---|---|---|
| 等待上传 | 上传卡和隐私说明 | 选择照片 |
| 本地读取 | 缩略图骨架、读取文案 | 取消 |
| 临时上传 | 显示真实上传状态，不伪装成主体识别 | 取消上传 |
| 排队等待 | 已获得 jobId，说明正在等待处理 | 取消任务、离开后回来查看 |
| 主体识别 | queued/running 对应的阶段文案和非虚假进度 | 取消任务 |
| 无主体 | 原图和原因说明 | 重试、换照片 |
| 超时 | “处理时间较长” | 重试、取消 |
| 限流 | 显示可重试时间 | 稍后再试 |
| 部分失败 | 成功贴纸保留，失败卡标记 | 单独重试 |
| 一句话额度耗尽 | 游客说明邀请码累计体验已用尽；注册账号说明本图已用尽，不显示下月重置；保留点选/框选 | 点一下、框一下或登录/查看权益 |
| AI 文案失败 | 原草稿不变，预览区显示原因 | 重试、继续自己写 |
| AI 排版过期 | 候选标记基于旧页面，禁止直接应用 | 重新生成、保留手工页面 |
| 任务已取消/过期 | 不展示旧候选，不覆盖当前照片 | 重新提交当前照片 |

### 24.2 仓库

- 个人仓库空：上传照片、浏览官方贴纸库。
- 官方库加载失败：提示失败，不影响个人仓库。
- 贴纸 Blob 缺失：显示缺失占位和资产名，不静默删除页面实例。
- 删除被引用贴纸：说明只移出仓库，不破坏页面。

### 24.3 编辑器

- 空白页：轻提示“从贴纸或文字开始”，首次添加后消失。
- 书本加载失败：留在开书前状态，提供重试和返回书架。
- 保存失败：顶部持久错误，不显示成功归架。
- 空间不足：说明本地空间限制，提供进入仓库或书架管理的路径，不擅自删除。
- 页面资源部分缺失：其余元素照常加载，缺失项单独标记。

### 24.4 离线

【建议设计】

- 已加载手帐可继续编辑并保存本地。
- 贴纸制作、AI 文案、AI 排版和云同步入口显示离线；保留本地所选照片直至用户离开或刷新。
- 恢复联网后不自动发送照片，需用户再次确认开始识别。

### 24.5 本地与云端存储提示

【已确认/页面事实】游客首次完成手帐和书架附近提示：数据只保存在当前浏览器，清理浏览器数据后可能丢失；同时提供登录同步入口。登录用户显示最近同步时间，并说明完整原图不长期保存，跨设备恢复的是手帐、文字和已保存贴纸。

## 25. 可访问性和减少动态效果

### 25.1 键盘

【建议设计】

- 所有按钮、导航、封面、贴纸卡和书本可 Tab 聚焦。
- Enter/Space 激活；Escape 关闭面板、取消文字编辑或跳过开书动画。
- 画布元素聚焦后可通过方向键小步移动；是否加入 Shift 大步移动为 P2。
- Delete/Backspace 删除选中元素前需避免与文字输入冲突。

### 25.2 焦点

【建议设计】使用清晰焦点环，不仅依赖 hover。打开对话框后焦点进入对话框，关闭后返回触发控件；删除后焦点转到合理相邻对象。

### 25.3 语义与读屏

【建议设计】

- 上传区使用真实文件输入与可访问标签。
- 保存状态和 Toast 使用适当 live region，避免重复朗读每次拖动。
- 纸张缩略块、候选主体和纯图标按钮提供文字名称。
- 页面缩略图声明页码和是否有内容。
- 装饰纹理、邮戳和胶带对读屏隐藏。

### 25.4 对比与触控

【建议设计】正文与背景达到 WCAG AA；选中状态同时使用轮廓/勾选和颜色。移动端触控目标至少 44×44 CSS px，危险操作与常用操作保持间距。

### 25.5 减少动态效果

【已确认/页面事实】遵循系统 prefers-reduced-motion。取消持续漂浮、滚动线、书本大位移、3D 翻转和页面翻动；用短淡入保持状态连续性。跳过动画按钮在默认模式下也必须存在。

## 26. 设计验收检查清单

### 26.1 范围

- [ ] 产品名称明确标记为暂定。
- [ ] 包含邀请码、游客、手机号登录、云同步、会员兑换和管理员后台；没有在线支付、自动续费、社区、公开分享或实体打印入口。
- [ ] 官方贴纸库被表达为官方内置素材，不含用户公开内容。
- [ ] 包含 AI 文案和会员 AI 排版；没有 AI 整本生成、贴纸重绘或复杂滤镜。
- [ ] 不包含自动分类、自定义贴纸包、绘画画笔或用户作品导出入口；不误删既有管理员导出及账号数据权利入口。

### 26.2 主流程

- [ ] 新用户能从首页完成上传、选主体、做贴纸、入仓库、选书、编辑和归架。
- [ ] 回访用户从书架重开先翻阅；主动点击“编辑”后才能修改；新建空白手帐可直接编辑。
- [ ] 个人仓库为空时仍能创建纯文字手帐。
- [ ] 导航可直达各区块，不强制用户按顺序浏览。

### 26.3 多主体与贴纸

- [ ] 候选主体在原图上清晰可选，重叠候选有替代选择方式。
- [ ] 用户可多选；N 个主体分别生成 N 枚贴纸。
- [ ] 不出现组合贴纸或逐像素高级精修；点选、框选、轮廓提示后 AI 自动贴边和一句话补救入口清楚可用。
- [ ] 白边预览背景能看清透明边缘。
- [ ] 个人与官方贴纸库视觉和语义清楚区分。
- [ ] 重命名、删除和被引用贴纸的提示符合规则。

### 26.4 编辑器

- [ ] 画布是单页平面编辑，不伪装真实双页编辑。
- [ ] 翻阅时无编辑工具且不可修改内容；模式切换不改变构图，跳过动画也不能绕过翻阅。
- [ ] 贴纸可选择、移动、缩放、旋转、复制、删除和完整调整层级。
- [ ] 文字可创建并二次编辑，样式范围有限且清楚。
- [ ] 页面可新增、切换、排序和删除，至少保留一页。
- [ ] 每页纸张独立，切换纸张不改变其他页或元素。
- [ ] 桌面和移动端都能完成核心操作；移动端不依赖双指手势。

### 26.5 保存与书架

- [ ] 正在保存、已保存、失败和空间不足状态可区分。
- [ ] 只有真实写入成功才显示“已保存”。
- [ ] 保存失败时不会播放完整成功归架动画。
- [ ] 书架支持打开、重命名和永久删除确认。
- [ ] 游客本地限制、登录同步范围和完整原图不长期保存说明在首次使用与书架区域可见。
- [ ] 游客本地保存、登录同步中、已同步和同步失败状态含义清楚。
- [ ] 完整原图不长期保存的说明可访问。

### 26.6 AI、账号与管理员

- [ ] AI 文案只读取当前页约定输入，先预览后应用，应用后仍可编辑并保留 AI 辅助来源。
- [ ] AI 排版四种风格可选，一次最多三案；新页只用用户所选素材和已有文案，当前页不增删内容；锁定元素不动，可撤销。
- [ ] 未绑定邀请码可跨浏览器进入并共享额度；绑定一个手机号后立即失效；已有账号可直接手机号验证码登录，产品没有密码入口。
- [ ] 游客作品由用户勾选导入，云端成功前保留本地副本，不未经确认自动上传。
- [ ] 游客累计体验、注册账号每图选主体和月度文案/排版分开展示；有效结果扣次、失败释放一致，只有月度额度按北京时间自然月重置。
- [ ] 游客同图成功用 2 次后注册免费账号，本图剩 1 次、新图有 3 次；同图用量不因跨月或刷新清零，文案按月衔接。
- [ ] 会员兑换按未到期顺延、已到期从兑换时起算，多个有效码可累计时长。
- [ ] 照片直传与异步任务的上传、排队、运行、取消、失败、成功和过期状态可理解。
- [ ] 账号菜单提供退出及账号与数据入口；注销/删除请求的身份复核和处理状态可见。
- [ ] A 退出后其本地作品保留但游客/B 不可查看或通过引用访问；再次登录 A 可恢复已保存内容，待同步任务不串号。
- [ ] 普通用户无法进入或调用管理员能力；高风险管理操作有确认与审计。
- [ ] API Key 仅能写入且之后掩码显示，不出现在网页源码、日志或导出中。

### 26.7 动画

- [ ] 取书、开书、翻页、合书和归架有统一状态机。
- [ ] 动画期间的操作锁定范围明确。
- [ ] 跳过动画始终可用，跳过后状态正确。
- [ ] 低性能降级不影响功能。
- [ ] 减少动态效果路径不包含大位移、翻转和持续循环。

### 26.8 视觉

- [ ] UI 呈现“现代编辑出版感的私人贴纸书”，不直接沿用旧原型视觉。
- [ ] 奶油白、暖纸灰、墨黑和单一强调色方向一致，没有仿真木板书架或儿童化咕卡感。
- [ ] 书架以封面画廊或书脊列表呈现，封面和用户作品是主要视觉资产。
- [ ] 编辑器默认保持精简，对象工具只在选中后出现，移动端没有压缩的桌面工具栏。
- [ ] 纹理不会影响可读性或移动端性能。
- [ ] 选中、成功、错误和危险操作不只依靠颜色区分。
- [ ] 页面留白充足，没有企业后台感或拥挤单屏感。

### 26.9 可访问性

- [ ] 所有主流程可通过键盘到达。
- [ ] 焦点顺序、焦点返回和对话框焦点管理正确。
- [ ] 图标按钮、纸张和候选主体具有可访问名称。
- [ ] 触控目标尺寸达标，危险操作不易误触。
- [ ] 保存反馈不会对读屏造成高频重复播报。

---

## 27. 当前待决策设计项

【未知/尚未确认】以下事项不妨碍 MVP 设计和开发启动，但在视觉定稿或技术联调前需要收敛：

1. 正式名称、Logo、品牌字标与最终品牌文案。
2. 正式封面数量、纸张数量和官方贴纸数量。
3. 图片格式、大小、像素与输出分辨率限制。
4. 主体候选展示数量上限。
5. 白边连续滑杆是否进入 P0。
6. 新页面默认空白纸还是继承最近纸张；当前建议空白纸。
7. 是否允许已创建手帐更换封面。
8. 文字是否开放额外颜色；当前建议不开放。
9. 是否实现通用撤销/重做；当前不属于已确认 MVP。
10. 正式桌面、平板、移动断点和支持浏览器版本。
11. 临时原图与 AI 中间文件的最长保留时间和用户可见文案。
12. 手机短信签名与模板的个人主体审核结果；开发环境只使用隔离测试验证码。
13. 首发模型主备路由和模型失败时的具体降级提示。
14. 管理员二次验证方式，以及首位管理员的部署期初始化流程。
15. 账号注销冷静期、云端数据删除时限和法定日志保留边界。
16. AI 生成合成内容标识、投诉举报和相关备案/安全评估对拾页的具体适用方式。
17. 免费用户和会员的云端对象存储配额、超限提示和清理入口。
